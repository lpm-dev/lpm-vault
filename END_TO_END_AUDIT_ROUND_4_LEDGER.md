# End-to-end audit round 4 ledger

Concept branch: `codex/end-to-end-vault-audit-round-4`

Base commit: `7abb251`

Implementation commits: Swift `d5590d7`; CLI `05f3270e` and `45f6f36b` (plus prerequisite `f645948a`); Registry `e1c668be`; documentation `52f6b08` and date snapshot `5a6e401`.

Coordinated pull requests: [Swift](https://github.com/lpm-dev/lpm-vault/pull/19), [CLI](https://github.com/lpm-dev/rust-client/pull/689), [Registry](https://github.com/tolgaergin/a-package-manager/pull/157), and [documentation](https://github.com/lpm-dev/rust-client-docs/pull/194).

The inherited audit used three read-only subagents to audit the complete version-controlled production and test surface for correctness, security, and performance. The primary agent independently reproduced those reports before changing production code. The local integration continuation used no subagents.

## Finding ledger

| ID | Source | Category | Location | Claim | Evidence | Disposition | Coverage | Commit | PR status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| C-R1-001 | correctness_round1 | Correctness | `VaultStore` personal and organization sync dispatch paths | Authority can become stale during pre-request suspension, but the cloud mutation is still dispatched and its response discarded. | A gated encryptor reproduced the stale dispatch; current code revalidates the captured authority immediately before remote admission. | Verified | `authority invalidation during personal encryption prevents cloud dispatch`; related organization invalidation tests | Implementation commits above | Review in concept PRs |
| C-R1-002 | correctness_round1 | Correctness | `AuthSessionCoordinator.clear` and `VaultStore.logout` | A failure after durable authority revocation is mapped as a generic storage failure, so the visible identity remains. | The coordinator now distinguishes `sessionRevokedWithCleanupFailure`, and the store publishes signed-out state for that terminal result. | Verified | `credential deletion failure after authority revocation reports a revoked session`; `a revoked session with cleanup failure clears stale identity and org routing` | Implementation commits above | Review in concept PRs |
| C-R1-003 | correctness_round1 | Correctness | `VaultStore.login` and identity loading | Login can validate and persist a new session, then return success after a second identity request fails while the UI retains an old or nil identity. | The login result now carries the validated identity through the session commit and does not depend on a second fetch. | Verified | `login cannot succeed with an identity from the previous session`; `startup identity load performs one user request and no token inventory requests` | Implementation commits above | Review in concept PRs |
| C-R1-004 | correctness_round1 | Correctness | `VaultStore` queued environment mutations | A completed mutation for a no-longer-selected project can change the current project's global environment selection. | A blocked project-A mutation followed by navigation to project B reproduced the redirect; publication now changes selection only when the captured project still owns navigation. | Verified | `a queued duplicate for another project cannot change the current environment` | Implementation commits above | Review in concept PRs |
| C-R1-005 | correctness_round1 | Correctness | `VaultWorkspaceView` mode synchronization | Environment navigation labels can retain the old mode payload while edits target the store's new environment. | Workspace mode now synchronizes its environment payload whenever the selected environment changes. | Verified | `environment workspace mode follows the selected environment` | Implementation commits above | Review in concept PRs |
| C-R1-006 | correctness_round1 | Correctness | `AuthSessionCoordinator.currentAccessToken` | Missing expiry metadata is treated as proof that an access fallback is unexpired after a refresh failure. | Removing the expiry file reproduced fallback authorization; unknown expiry now fails closed after refresh failure. | Verified | `missing expiry metadata cannot authorize access after refresh failure` | Implementation commits above | Review in concept PRs |
| C-R1-007 | correctness_round1 | Correctness | Pull merge key counts and status text | The reported key count mixes merged keys in remote environments with omission of local-only environments, so it represents neither remote nor final totals. | Merge results now count only the validated remote payload while retaining the complete merged environments separately. | Verified | `pull key counts represent keys received from the remote payload` | Implementation commits above | Review in concept PRs |
| S-R1-001 | security_round1 | Security | `BiometricService.authenticate` and `resetCache` | An authentication started before lock can complete afterward and restore the cache for a later unlock. | A suspended authentication completed after `resetCache` and previously restored cached authorization; generation validation now suppresses the stale cache write. | Verified | `cache reset invalidates authentication attempts already in flight` | Implementation commits above | Review in concept PRs |
| S-R1-002 | security_round1 | Security | `BoundedEnvFileReader` | Dotenv imports follow final-component symbolic links because both readers omit `O_NOFOLLOW`. | Both descriptor-open paths now use `O_NOFOLLOW` and map `ELOOP` to `notRegularFile`. | Verified | `bounded dotenv readers reject final-component symbolic links` | Implementation commits above | Review in concept PRs |
| P-R1-001 | performance_round1 | Performance | Streaming multiline dotenv parsing | Every physical line rescans and recopies the full pending quoted assignment, producing quadratic runtime. | The parser now carries quote state and scans only each appended line; 6,000 vs 12,000 continuation-line medians were 8.39 ms vs 17.11 ms on the current tree. | Verified | `multiline dotenv scaling`; multiline compatibility and limit regressions | Implementation commits above | Review in concept PRs |
| P-R1-002 | performance_round1 | Performance | `SharedKeychainStore.write` | Protected-only writes read and materialize the old payload even though rollback cannot use it. | Protected-only writes no longer snapshot an unused previous value; the remaining single read is the post-write verification read. | Verified | `protected-only writes read the payload only for verification` | Implementation commits above | Review in concept PRs |
| P-R1-003 | performance_round1 | Performance | Completed protected-only cutover | Stable-key access rereads both irreversible cutover markers after the state is cached. | Completed cutover state is cached under the store lock and repeated checks perform no backend reads. | Verified | `protected-only state is cached after one durable marker read`; `a cached completed cutover performs no further backend reads` | Implementation commits above | Review in concept PRs |
| P-R1-004 | performance_round1 | Performance | `VaultPersistenceCoordinator.persistMutation` | One project mutation reads and decodes the monolithic sync-metadata account, then rereads the same raw bytes for rollback. | Metadata loading now returns the decoded value and exact raw snapshot together, eliminating the second backend read. | Verified | `single-project mutation reads only its target project`; metadata rollback regressions | Implementation commits above | Review in concept PRs |
| P-R1-005 | performance_round1 | Performance | Workspace dotenv export | Formatting, allocation, writing, and `fsync` execute synchronously on the main actor. | Export formatting and durable writing now run in a bounded background service; a 32 MiB export measured a 2.84 ms maximum main-actor heartbeat gap. | Verified | `dotenv export work runs away from the main actor`; `dotenv export main-actor gap` | Implementation commits above | Review in concept PRs |
| P-R1-006 | performance_round1 | Performance | `VaultCrypto.decrypt` | Reconstructing `nonce + ciphertext + tag` creates a removable full-ciphertext copy. | Decryption now builds `AES.GCM.SealedBox` from nonce and Data slices without allocating a combined ciphertext copy; current 7.5 MiB median is 4.09 ms. | Verified | `AES-GCM decrypt time and peak RSS`; crypto round-trip suites | Implementation commits above | Review in concept PRs |
| P-R1-007 | performance_round1 | Performance | `SyncService.listProjects` pagination | Per-page response limits allow roughly one GiB of retained project strings across valid pages. | Pagination now shares one 10 MiB response budget and a 10,000-project ceiling across all pages and rejects cursor cycles. | Verified | `cloud env project listing enforces one cumulative response budget`; `project pagination cumulative budget` | Implementation commits above | Review in concept PRs |
| P-R1-008 | performance_round1 | Performance | `LPMAPIService.fetchTokens` and account inventory | Per-page limits and concurrent organization inventories allow roughly one GiB of retained token data. | Token pagination now shares one 2 MiB/10,000-token budget, and organization inventories use a four-request sliding window with an aggregate token ceiling. | Verified | `token inventory enforces one cumulative response budget`; `aggregate token inventory rejects excessive organization payloads`; `organization token inventory uses a four-request sliding window` | Implementation commits above | Review in concept PRs |
| S-R2-001 | security_final | Security | `OrganizationMemberAuthorizationPolicy.prepare` and organization approval persistence | Low-order X25519 member keys can be approved and persisted as trusted before wrapping rejects them. | Canonical all-zero and `01 00…00` keys produced pending approvals before the fix; preparation now performs a contributory key agreement once per distinct key. | Verified | `organization sharing rejects non-contributory member keys before approval` | Implementation commits above | Review in concept PRs |
| S-R2-002 | security_final | Security | Default sessions in `LPMAPIService`, `SyncService`, and `LoginService.exchange` | Drip-fed responses can retain tasks and buffers until Foundation's seven-day default resource timeout. | Default ephemeral sessions had 60-second inactivity and 604,800-second resource timeouts; they now use explicit 15/60, 30/120, and 15/30 second request/resource policies. | Verified | `default cloud sessions enforce finite request and resource deadlines`; bounded streaming response tests | Implementation commits above | Review in concept PRs |
| C-FRESH-001 | correctness_final | Correctness | Vault `VaultStore` personal and organization pull commit paths | A pull superseded by project navigation or a newer sync operation can persist its remote payload after its captured authority is no longer current. | Gated personal and organization pulls were paused immediately before persistence, then superseded; both formerly reached the durable commit because the full operation authority result was discarded. Commit admission now requires the captured account, project, authorization generation, and operation generation to remain current. | Verified | `superseded personal pull cannot enter its durable commit`; `superseded organization pull cannot enter its durable commit` | Implementation commits above | Review in concept PRs |
| C-FRESH-002 | correctness_final | Correctness | CLI personal push, organization share, personal rotation, and organization rotation metadata commits | A successful remote write can publish its revision metadata into a replacement local vault when `lpm.json` changes while the request is in flight. | The four metadata paths wrote by project path without revalidating the captured vault ID. They now use the vault-matching metadata commit primitive, which rejects a changed or replaced manifest binding. | Verified | `personal_metadata_write_rejects_a_replacement_vault`; `organization_metadata_write_rejects_a_replacement_vault`; existing rotation commit-guard coverage | Implementation commits above | Review in concept PRs |
| C-FRESH-003 | correctness_final | Correctness | CLI organization push contract and Registry organization vault mutation transaction | A mutable organization slug can be rebound while a write is in flight, redirecting ciphertext to a different immutable organization. Maintainer pushes also encrypted before checking the pulled organization principal. | CLI member-key discovery already returns the immutable organization ID but the write body omitted it. The CLI now sends `expectedOrganizationId`, validates maintainer pull principals before encryption, and the Registry rejects an ID mismatch under the organization transaction lock. | Verified | `maintainer_push_rejects_a_changed_organization_before_posting`; organization request-body assertion for `expectedOrganizationId`; `rejects a write when the locked organization ID differs from the client precondition`; route pass-through coverage | Implementation commits above | Review in concept PRs |
| C-FRESH-004 | correctness_final | Correctness | Registry pairing stage, protocol-v2 approval, and legacy approval compare-and-swap transitions | A request that passed an application-time expiry check could wait and then transition an already-expired pairing session. | Each transition previously omitted expiry from its transactional predicate. All three now compare `expires_at` with database time; a zero-row transition distinguishes expiry with a database-time lookup and raises `PairingSessionExpiredError`, which routes map to HTTP 410. | Verified | `binds pending-to-confirming transition to the authenticated user`; `binds approval to the user and exact staged public key`; `binds legacy approval to database-time session expiry`; `reports an expired session when the database-time CAS rejects it`; stage and final approval HTTP 410 tests | Implementation commits above | Review in concept PRs |
| C-FRESH-005 | correctness_final | Correctness | Vault organization pull-conflict recovery and conflict-resolution views | Organization conflicts opened generic recovery actions that invoked personal-only pull and force-push operations, leaving recovery inert. | A captured organization conflict target reproduced a personal dispatch with no organization request. Recovery now dispatches by the captured account and the policy suppresses force-push for organization conflicts. | Verified | `organization conflict recovery retries its organization pull without force push`; personal conflict contract coverage | Implementation commits above | Review in concept PRs |
| P-FINAL-001 | performance_final | Performance | Registry browser and CLI mutation commit authorization | Each locked authorization row was immediately read again, doubling database round trips and extending transaction time. | The browser path performed four calls for two rows and the CLI path six calls for three rows. Each row lock now returns the required projection directly: two browser calls and three CLI calls, with the original lock order and modes preserved. | Verified | `revalidates a browser user and Supabase session in lock order`; `revalidates CLI user, session, and exact bearer token in lock order` assert total database-call counts and lock modes | Implementation commits above | Review in concept PRs |
| P-FINAL-002 | performance_final | Performance | Vault non-force personal push | Every normal push made a version-preflight HTTP request even though its durable checkpoint already supplies the compare-and-swap revision. | Cold and previously-synced non-force pushes now use the durable checkpoint directly and issue one write request; force pushes retain authenticated version preflight and race recovery. | Verified | `non-force personal pushes use the durable checkpoint without a preflight`; force-push preflight and race regressions | Implementation commits above | Review in concept PRs |
| P-FINAL-003 | performance_final | Performance | Vault organization push key validation | Organization push fetched the caller's public key separately after already receiving the same binding in the organization member-key inventory. | The push now validates the local device key against its member-inventory entry, eliminating one HTTP request while preserving invalid, missing, stale, and duplicate binding rejection. | Verified | `organization push validates its device key from the member inventory`; `organization push rejects invalid current-member key bindings` | Implementation commits above | Review in concept PRs |
| C-FRESH-006 | correctness_final | Correctness | Registry pairing stage, protocol-v2 approval, and legacy approval expiry predicates | PostgreSQL `CURRENT_TIMESTAMP` is fixed at transaction start, so a transaction begun before expiry can wait on commit authorization or a row lock and still transition the session after wall-clock expiry. | The strengthened SQL-contract regression failed all four transition/diagnostic checks until each predicate used PostgreSQL `clock_timestamp()`; the focused pairing and organization-access suites then passed 13 tests. | Verified | `vault-pairing-cas.test.js` wall-clock expiry predicate coverage | Implementation commits above | Review in concept PRs |
| C-FRESH-007 | correctness_final | Correctness | Vault `SyncService` organization push request and `OrgSyncServiceProtocol` | The Registry requires `expectedOrganizationId`, but the Swift client did not send it, so every Vault app organization push was rejected at the route boundary. | The request-contract regression first failed compilation because the API lacked the immutable ID. The required parameter now propagates through the protocol, concrete service, mock, and store, and the focused current-contract suite passes. | Verified | `organization push includes the immutable organization identity` | Implementation commits above | Review in concept PRs |
| P-CLEAN-001 | performance_final | Performance | Registry organization env authorization | Transactional authorization reread a row immediately after locking it; list, pull, push, delete, audit, and explicit-slug platform paths also resolved an organization by slug and then reselected the same organization by ID. | Transactional authorization now evaluates the lock projection directly. All six preloaded-org paths reuse the full row returned by `getOrgBySlug` and perform only the membership query; 86 focused authorization, route, and control-plane tests pass. | Verified | SHARE/UPDATE lock query counts, resolved-org no-reread, list-route and explicit-slug platform reuse coverage | Implementation commits above | Review in concept PRs |
| P-CLEAN-002 | performance_final | Performance | CLI organization share and content-key rotation public-key readiness | Steady-state share and rotation fetch `/api/users/me/public-key` before fetching member public-key inventory, although that inventory already contains the caller's authoritative key binding. | The organization member inventory now carries the authenticated caller identity and authoritative caller key binding. Share and rotation classify readiness from that one response, eliminating the serial public-key request without weakening stale, missing, or changed-key rejection. | Verified | `env_share_uses_one_caller_bound_member_inventory_without_a_public_key_preflight`; changed-key and member-inventory error workflow cases; organization rotation request-count coverage | Implementation commits above | Review in concept PRs |
| P-CLEAN-003 | performance_final | Performance | CLI sync-payload parsing and Rust vault personal pull helpers | Owned parsed secret maps were deep-cloned while their source wrappers remained live, duplicating plaintext strings and adding O(payload) allocation/copy cost. | All three paths now remove and move the owned environment maps. Pointer-identity regressions prove the selected `String` allocations are transferred unchanged, while missing wrapper/environment and legacy-flat behavior is preserved. | Verified | Four ownership/absence tests plus existing legacy-flat pull coverage | Implementation commits above | Review in concept PRs |
| P-CLEAN-004 | performance_final | Performance | CLI environment inventory schema validation | Every rendered environment cloned an effective secret map solely to call `contains_key`, repeatedly cloning the default map for empty environments and aliases. | Schema validation now borrows the environment-specific or shared default map. Reference-identity regressions cover default, non-empty environment, empty fallback, missing environment fallback, and absent default. | Verified | `effective_schema_vars_borrow_environment_specific_and_default_maps`; `effective_schema_vars_is_absent_without_a_default_fallback` | Implementation commits above | Review in concept PRs |
| P-CLEAN-005 | performance_final | Performance | Vault desktop pulls and imports plus CLI organization pull | Desktop store pulls, desktop organization cloud imports, and CLI organization pulls fetch the caller public key before a pull whose signed response is already server-gated and bound to that same key. | Successful pulls now validate the signed recipient key version and fingerprint directly against the local key. Public-key discovery runs only for the bounded missing-key registration fallback, removing one steady-state HTTP request from desktop pull/import and CLI organization pull. | Verified | `other organization pull errors do not fetch the public key`; unauthorized and malformed-response variants; steady-state and bounded registration workflow request counts | Implementation commits above | Review in concept PRs |
| S-FINAL-001 | security_final | Security | Registry vault-OIDC exchange and generic Registry token authorization | A credential minted for one vault CI pull was stored as a generic `read` token for the policy owner and accepted by package, account-inventory, token-rotation, organization-membership, entitlement, and pool paths. | Vault exchange now mints a distinct `vault:read` capability. Generic raw/hash validators reject both the new scope and legacy vault provenance; only a structurally bound dedicated validator admits CI pull. Package scope helpers fail closed as defense in depth, and 284 focused tests pass. | Verified | Generic-vs-dedicated validation, legacy provenance, scope isolation, exchange, CI pull, catch-all, and token rotation suites | Implementation commits above | Review in concept PRs |
| S-FINAL-002 | security_final | Security | Registry vault-OIDC GitHub ref-type and branch authorization | GitHub tag refs are collapsed into the same bare namespace as branch refs, while the exchange does not validate GitHub `ref_type`; a tag named like an allowed protected branch can satisfy the branch policy. | Exchange now extracts and requires the exact `branch` ref type before branch policy evaluation, records it in token provenance, and CI pull independently rejects legacy or non-branch provenance before decrypting. | Verified | GitHub and GitLab tag, missing, and unknown `ref_type` exchange cases; genuine branch exchange; CI-pull provenance rejection matrix | Implementation commits above | Review in concept PRs |
| S-FINAL-003 | security_final | Security | Vault `SyncMetadata` and persistence admission; CLI durable sync bindings, pull/push/share/rotation entry points | A signed response-selected principal could replace the local principal namespace and bypass the prior principal's rollback checkpoint, allowing replay or silent account/organization lineage switching. | Both clients now reject an active same-Registry/scope principal conflict before network mutation, decryption, or durable commit. The rollback floor is the maximum recorded revision across all principals for that Registry and scope, legacy unbound floors remain fail-closed, and distinct Registries remain independent. | Verified | `personal pull rejects a different active principal before network or decryption`; `sync rollback floors survive principal and Registry switches`; Rust active-principal conflict and cross-principal Registry-floor tests; `env_pull_rejects_a_different_bound_account_before_local_overwrite` | Implementation commits above | Review in concept PRs |
| S-FINAL-004 | security_final | Security | Vault and CLI authenticated personal/organization pull and import decrypt paths; Registry dashboard create/save encryption | Network protocol v1/v2 ciphertext carries no authenticated revision, so accepting or auto-migrating it trusts a replayable server-selected revision and can overwrite current state. | Authenticated pull envelopes in both clients now require protocol v3 before ciphertext extraction/decryption, all automatic remote legacy migration paths are removed, and Registry dashboard creation/save always requires protocol v3 with creation ciphertext bound to revision 1. Explicit offline legacy primitives remain available only outside network sync. | Verified | Swift network v1/v2 personal, organization, and import rejection tests; Rust authenticated-envelope and no-migration pull regressions; Registry protocol-v3 creation/save and revision-1 tests | Implementation commits above | Review in concept PRs |
| C-REAUDIT-001 | correctness_final | Correctness | CLI durable organization authority checkpoints in `vault_id.rs` | Organization rollback floors are keyed by Registry and principal but not by organization slug, so a high revision for one organization can reject the first lower revision committed for an independent organization at the same Registry. | Persisting `acme` revision 50 and then independent `umbrella` revision 1 at the same Registry reproduced the rollback rejection. Organization checkpoints now nest Registry/principal floors under the organization slug. | Verified | `organization_checkpoint_floors_are_independent_across_slugs`; `active_organization_principal_cannot_commit_below_same_slug_registry_floor`; same-slug principal conflict regression | Implementation commits above | Review in concept PRs |
| P-STABLE-001 | performance_final | Performance | CLI personal push and rotation preflights; Registry personal mutation principal admission | Personal mutations add redundant signed version requests because the POST does not carry a principal precondition that could safely replace them. | Request-count regressions reproduced one extra version GET for a bound normal push and one after rotation's signed pull. Personal mutations now carry `expectedPrincipalId`, the Registry rejects mismatches, non-force pushes use the durable checkpoint, rotation reuses its signed pull, and force retains one bounded preflight per attempt. | Verified | `env_push_uses_bound_checkpoint_without_a_version_preflight`; `env_rotate_key_preserves_complete_named_remote_payload_with_cas`; `bound_force_push_reads_the_current_revision_before_encrypting`; Registry mismatched-principal route regression | Implementation commits above | Review in concept PRs |
| P-STABLE-002 | performance_final | Performance | Vault organization member preparation, approval, and content-key wrapping | Validated recipient keys are passed back as raw wire records, causing every approved recipient to be decoded and fingerprinted again during wrapping. | Instrumentation reproduced two decodes and two fingerprints per recipient. Preparation now returns typed validated recipients and wrapping consumes their decoded keys and canonical fingerprints directly. At 1,024 recipients the Release median fell from 0.790208 ms to 0.002333 ms (about 339x) with 26,312,704-byte peak RSS. | Verified | `organization recipient keys are decoded and fingerprinted once before wrapping`; contributory-key rejection coverage; `organization recipient validation handoff` Release benchmark | Implementation commits above | Review in concept PRs |
| P-STABLE-003 | performance_final | Performance | Registry organization member inventory, recipient commit validation, access validation, and public-key persistence | Stored X25519 keys are reparsed with a synchronous contributory scalar multiplication whenever their fingerprint is needed. | Call-count tests reproduced redundant validation on stored-key reads. Registration and rotation now validate once and persist a required full fingerprint protected by migration 0171 canonical-key and digest invariants; reads compare the stored digest without scalar multiplication. Median inventory/validation time changed from 1.330 ms to 0.001 ms at 1 member, 638.983 ms to 0.007 ms at 1,024, and 7,683.417 ms to 0.016 ms at 10,000. | Verified | Stored-fingerprint read tests; registration/rotation persistence tests; `user-public-key-fingerprint-migration.test.js`; `benchmark-vault-public-key-fingerprints.mjs` | Implementation commits above | Review in concept PRs |
| P-STABLE-004 | performance_final | Performance | Registry personal vault sync route and transactional mutation | A successful personal mutation reads vault write state once before the transaction and again inside the authoritative transaction. | Route and query-count regressions reproduced the duplicate read. Current writes now require `wrappedKey` and all authoritative version, revision, principal, and downgrade checks execute from the single transaction-owned state read; conflict rereads remain only for genuine races. | Verified | `leaves the only write-state read inside the authoritative transaction`; personal create/update query-count and conflict-contract tests | Implementation commits above | Review in concept PRs |
| S-STABLE-001 | security_final | Security | Rust and Swift retained principal checkpoints across Registry detours | Returning to a previously used Registry after switching Registries could have allowed a different principal to replace the retained authority namespace. | Duplicate of S-FINAL-003. The current Rust and Swift checkpoint histories resolve retained same-Registry principals, reject substitution, preserve rollback floors, and fail closed at capacity. | Verified | Covered by S-FINAL-003 principal-detour, cross-principal floor, Registry independence, and capacity regressions | Implementation commits above | Review in concept PRs |
| S-STABLE-002 | security_final | Security | Registry sync GETs, dashboard decrypt/save, CI decrypt, and vault query crypto-version defaults | Registry-owned read and decrypt surfaces still accept protocol v1/v2 ciphertext whose revision is unauthenticated, allowing replayed legacy plaintext to be served, exposed to CI, or recommitted as fresh v3 state. | Direct personal and organization GETs, dashboard save/decrypt, CI pull, and omitted-version query fixtures reproduced legacy admission. Registry-owned network and dashboard surfaces now require protocol v3 and return the stable retired-version error before ciphertext release or decryption; omitted stored versions default only to the retired value and fail closed. | Verified | Personal and organization route legacy rejection matrices; `rejects legacy ciphertext before CI decryption`; dashboard rollout retirement tests; escrow pre-key-access rejection; sync-envelope legacy rejection | Implementation commits above | Review in concept PRs |
| S-STABLE-003 | security_final | Security | Cross-language protocol-v3 AAD in Rust, Swift, and Registry browser/CI crypto | Protocol v3 authenticates scope, vault ID, organization slug, and revision but omits the immutable user or organization principal, so first-use ciphertext can be reassociated across principals when wrapping keys remain usable. | Principal-only substitution reproduced successful decryption before the fix. Protocol-v3 AAD now includes the immutable user or organization ID in Swift, Rust, Registry browser crypto, escrow/CI crypto, and signed response envelopes; cross-language vectors agree and a principal-only substitution fails authentication. | Verified | Swift and Rust protocol-v3 cross-language vectors and principal-substitution tests; Registry `binds protocol-v3 associated data to the immutable principal`; escrow owner-substitution and signed-envelope principal tests | Implementation commits above | Review in concept PRs |
| C-PRIMARY-001 | primary_verification | Correctness | Vault Release test compilation in `AuthSessionCoordinator`, `KeychainService`, and `SyncService` | Release tests referenced internal verification seams compiled only under `#if DEBUG`, so the Release benchmark target did not compile and could not provide production-optimization evidence. | `swift test -c release --filter Round4BenchmarkTests` failed at compile time until the internal seams were available in Release builds. The same command now compiles and all six Release benchmarks pass; `swift build -c release` also passes with zero warnings. | Verified | Release build plus six `Round4BenchmarkTests`, including the 1,024-recipient and peak-RSS measurements | Implementation commits above | Review in concept PRs |
| C-PRIMARY-002 | primary_verification | Correctness | Registry `CreateVaultProject` authenticated-encryption callback | The memoized submit callback omitted `principalId`, so a session/principal rerender could encrypt a new vault under the stale account identity. | A stable-router rerender from `account-1` to `account-2` after form edits reproduced `account-1` in the protocol-v3 AAD. Adding `principalId` to the callback dependencies makes encryption use the current authenticated account. | Verified | `uses the current authenticated principal after a rerender` | Implementation commits above | Review in concept PRs |
| C-PRIMARY-003 | primary_verification | Correctness | Vault CLI compatibility descendant-lifetime regression | The descendant-process test used a 0.5-second total deadline plus an artificial startup delay, so a loaded parallel run could time out before `child.pid` existed and report an unrelated file-read failure. | One full parallel run reproduced the missing PID file; the isolated test and the next full run passed. Removing the artificial delay and retaining a one-second prompt-return bound under a two-second probe deadline passed 20 consecutive repetitions. | Verified | `CLI version probing bounds output, time, and descendant lifetime`, repeated 20 times | Implementation commits above | Review in concept PRs |
| COR-CLEAN-001 | correctness_clean | Correctness | Vault queued cloud import commit admission | A cloud import queued before an account switch or task cancellation could persist into the replacement account after its original authority stopped being current. | Both account-switch and cancellation regressions persisted the imported project before the fix. Commit admission now rejects cancelled tasks and requires the captured account and principal in addition to the existing session, environment, and authorization generations. | Verified | `account switch before cloud import commit prevents persistence`; `task cancellation before cloud import commit prevents persistence`; post-durable cancellation/navigation regression | Implementation commits above | Review in concept PRs |
| COR-CLEAN-002 | correctness_clean | Correctness | Vault `SyncService` organization push failure decoding | Current Registry remediation envelopes were collapsed to `nil`, discarding actionable re-wrap, rotation, recipient-key, identity, slug, crypto, and role failures. | A response table reproduced `nil` for every current Registry failure. Strict status/code/error/field validation now preserves only the supported envelopes; malformed status, errors, versions, fields, hints, recipient IDs, and unknown codes fail closed. | Verified | `organization push preserves exact actionable Registry failures`; `organization push rejects malformed Registry failure envelopes` | Implementation commits above | Review in concept PRs |
| COR-CLEAN-003 | correctness_clean | Correctness | Registry approved pairing consumption and expiry cleanup | An approved pairing could release wrapping material after its TTL because consumption did not include expiry in its compare-and-swap predicate, and cleanup excluded expired approved rows. | Approved consumption after expiry succeeded before the fix. Consumption now requires `expires_at > clock_timestamp()`, diagnoses expired approved rows as `PairingSessionExpiredError`, maps them to `status: expired`, and cleanup includes expired approved sessions. | Verified | `reports an expired approved session when consumption loses the expiry CAS`; expired approved GET and cleanup regressions | Implementation commits above | Review in concept PRs |
| COR-CLEAN-R01 | correctness_clean | Correctness | CLI personal push principal precondition and Registry tenant scope | A reported lead claimed a principal could be substituted between personal push preparation and commit. | Rejected after tracing the captured bearer token through the request and the Registry's exact token user, expected-principal, and transaction-time mutation authorization predicates; no attacker-controlled principal substitution path exists. Existing token/principal race regressions cover the claimed transition. | Rejected | Existing personal mutation principal-precondition and exact bearer-token authorization suites | Not applicable | Not applicable |
| P-RESUME-001 | performance_final | Performance | Vault bounded HTTP response collection for avatar and updater downloads | The fallback collector advanced `URLSession.AsyncBytes` once per byte and copied bytes through an intermediate array, imposing one async-sequence transition per response byte. | The fallback loop was directly traceable in `BoundedHTTPResponse.loadStreaming`. Every production consumer and test fixture now installs a shared `URLSessionDataDelegate` collector, reducing dispatch from O(bytes) async element transitions to O(Foundation data chunks) callbacks while retaining the hard cap and direct `Data` append. | Verified | Exact-cap and over-limit chunked response tests; delegated avatar/updater configuration, cancellation propagation, and avatar redirect-policy tests | Implementation commits above | Review in concept PRs |
| P-RESUME-002 | performance_final | Performance | Registry organization vault POST authorization preflight | Organization writes queried membership/access and then issued a second vault write-state query before entering the authoritative transaction. | The focused regression failed when the second query was forced to throw. Authorization now returns membership and write state from one indexed joined projection, and the route consumes that projection. | Verified | `uses the authorization projection instead of a second pre-transaction write-state query` | Implementation commits above | Review in concept PRs |
| P-RESUME-003 | performance_final | Performance | Registry dashboard personal vault save preflight | Dashboard saves fetched ownership and then fetched the same vault again for crypto and wrapped-key state. | The focused regression failed when the redundant personal write-state query was forced to throw. One primary-key projection now supplies ownership, wrapped key, version, and crypto state. | Verified | `authorizes and prepares a dashboard save with one projected row lookup` | Implementation commits above | Review in concept PRs |
| P-RESUME-004 | performance_final | Performance | Registry account deletion and revoke-all pairing deletion | Pairing deletion materialized every deleted UUID in application memory solely to count rows. | Scalar-count regressions covered zero, one, and one million deleted rows. A data-modifying CTE now returns only `COUNT(*)`, and both callers consume the scalar result. | Verified | `returns the exact scalar count without materializing ... deleted IDs`; million-row bounded aggregate coverage; revoke-all audit count tests | Implementation commits above | Review in concept PRs |
| P-RESUME-005 | performance_final | Performance | Registry route and dashboard revoke-all notification delivery | User lookup and revocation email transport blocked the successful revoke-all response after the durable deletion. | Deferred-transport regressions captured the callback and proved the response returns before user lookup or email send. Both surfaces now schedule best-effort notification work with Next `after()` while preserving audit counts and failure isolation. | Verified | Route and action `returns before the revocation email transport starts` tests; audit/email failure coverage | Implementation commits above | Review in concept PRs |
| P-RESUME-006 | performance_final | Performance | Registry OIDC policy-list GET authorization | Generic platform authentication and the authorized organization list helper repeated vault ownership, organization, billing, and membership reads before the policy query. | The route regression failed until GET used the specialized path. Explicit and inferred scopes now use one joined authorization projection followed by one bounded policy query whose SQL still contains the current membership or personal entitlement predicate. | Verified | Specialized-auth tests for personal, explicit/inferred org, ambiguity, lapsed billing, wrong/missing vault, and membership removal; route empty-list and final predicate tests | Implementation commits above | Review in concept PRs |
| COR-CLEAN-004 | correctness_clean | Correctness | Registry personal force-create route and `upsertPersonalVault` | Two forced creates racing an absent personal env could both report success although only one ciphertext was stored. | The losing `ON CONFLICT DO NOTHING` path previously reread and acknowledged the competing row. First-party creates now set `failOnCreateConflict`, and an insert conflict raises the stable creation-conflict response instead of returning another writer's revision. | Verified | `fails a force-create race instead of acknowledging the winning ciphertext`; first-write and force update contract coverage | Implementation commits above | Review in concept PRs |
| COR-CLEAN-005 | correctness_clean | Correctness | Vault and CLI organization recipient-set acceptance | Accepted sharing-recipient trust could be reused across Registry and organization trust boundaries. | Acceptance storage and digests now include the canonical Registry authority and immutable organization ID. Cross-Registry and cross-organization probes cannot reuse an accepted digest, while an exact same-scope set remains durable. | Verified | `recipient trust is isolated by registry and immutable organization ID`; Swift cross-Registry and cross-organization acceptance regressions | Implementation commits above | Review in concept PRs |
| COR-CLEAN-006 | correctness_clean | Correctness | CLI forced personal push recreation | A bound checkout could not force-recreate a remotely deleted personal env for the same authenticated principal. | The exact signed missing-vault response now permits revision-one recreation only when the response principal matches the retained same-Registry binding; unrelated missing responses and substituted principals still fail closed. | Verified | `bound force push recreates an exactly missing vault for the same principal`; exact-missing and unrelated-not-found force-push regressions | Implementation commits above | Review in concept PRs |
| COR-CLEAN-007 | correctness_clean | Correctness | Registry OIDC token issuance and audit insertion | Credential creation and its mandatory audit event could commit independently. | Token insertion and the successful `oidc_exchange` audit now execute in the same Registry transaction; an audit failure rejects and rolls back the credential write. | Verified | `commits the OIDC token and success audit in one transaction`; OIDC exchange suite | Implementation commits above | Review in concept PRs |
| COR-CLEAN-008 | correctness_clean | Correctness | Vault project-name validation | Swift accepted names beyond the Registry's UTF-16 and control-character contract. | C0/C1 and over-200-UTF-16 regressions failed before local validation enforced the complete Registry contract. | Verified | `Round4AuditRegressionTests` project-name boundary, control, and Unicode coverage | Implementation commits above | Review in concept PRs |
| COR-CLEAN-009 | correctness_clean | Correctness | CLI OIDC dotenv output | Raw interpolation was lossy for special characters and direct writes could truncate existing output. | Round-trip and injected-write-failure regressions failed before canonical dotenv formatting and secure atomic replacement were used. | Verified | CLI OIDC, `lpm-env`, `lpm-runner`, and workflow dotenv regressions | Implementation commits above | Review in concept PRs |
| COR-CLEAN-010 | correctness_clean | Correctness | CLI OIDC request serialization | An absent environment selector serialized as `"env": null` instead of being omitted. | The exact workflow mock rejected the former request and the unit serialization regression failed before `skip_serializing_if` was added. | Verified | OIDC request unit tests and exact default-environment workflow match | Implementation commits above | Review in concept PRs |
| COR-CLEAN-011 | correctness_clean | Correctness | Registry organization audit, platform, and OIDC ownership during account deletion | Cascading actor FKs delete organization-owned history and integrations when a former member is purged. | Repository generation produced migration `0172`; its semantic snapshot diff is limited to the intended actor-FK, nullability, scope-check, and pairing-index changes. Applying it to a reconstructed pre-0172 schema, then performing personal cleanup and deleting the actor, retained all three organization-owned row types with null actor attribution. | Verified | Account deletion, cleanup, schema contract, migration-audit alignment, and direct generated-DDL lifecycle exercises | Implementation commits above | Review in concept PRs |
| COR-CLEAN-012 | correctness_clean | Correctness | Registry organization member removal and key rotation | Removing the final current, structurally usable wrapped-key holder could leave an organization env permanently undecryptable. | Membership removal, account deletion, and public-key rotation are serialized on affected organization rows. Revocation now checks each current target key against remaining current members, public-key versions/fingerprints, content-key versions, and wrapped-key structure; if any vault would be orphaned, no key deletion occurs and the surrounding transaction rolls back the membership change. | Verified | `rejects revocation of the final current organization key holder`; organization-lock ordering and revocation transaction suites | Implementation commits above | Review in concept PRs |
| PERF-CLEAN-001 | performance_final_clean | Performance | Registry pairing approval and revoke notification delivery | Notification lookup/transport can block successful responses after the durable mutation. | Current approval and single-revoke paths use deferred Next `after()` work; 135 focused pairing/action tests cover response-before-transport behavior. | Verified | Existing deferred notification regressions | Implementation commits above | Review in concept PRs |
| PERF-CLEAN-002 | performance_final_clean | Performance | Registry consumed pairing history | The consumed-history query needs an ordered partial index to avoid growing sort/scan cost. | Repository generation produced migration `0172` with `vault_dashboard_sessions_consumed_history_idx`; direct application confirmed the exact `(user_id, consumed_at DESC NULLS LAST, id DESC NULLS LAST) WHERE status = 'consumed'` definition. | Verified | `vault-account-lifecycle-and-pairing-index.test.js`; generated migration and physical-audit coverage | Implementation commits above | Review in concept PRs |
| PERF-CLEAN-003 | performance_final_clean | Performance | Registry organization env deletion transaction | Deletion repeated the organization env lookup inside the transaction. | The failure regression reproduced the duplicate lookup; transaction authorization now returns the locked env and 12 transaction tests pass. | Verified | Organization deletion transaction query-count coverage | Implementation commits above | Review in concept PRs |
| PERF-CLEAN-004 | performance_final_clean | Performance | Registry generic platform authentication | Personal and organization authorization used multi-query waterfalls. | Joined explicit/inferred projections now preserve plan, membership, ambiguity, selector, and fallback behavior; the focused query-count suite passes. | Verified | `platform-auth.test.js` one-projection and behavioral matrix | Implementation commits above | Review in concept PRs |
| PERF-CLEAN-005 | performance_final_clean | Performance | Registry organization env GET | Version-only and full pulls performed three to five SELECTs. | A single bounded projection now carries organization, membership, env, wrapped-key, and public-key state while preserving response distinctions; focused route tests pass. | Verified | `org-vault-routes.test.js` full/version projection and failure matrix | Implementation commits above | Review in concept PRs |
| PERF-CLEAN-006 | performance_final_clean | Performance | CLI cloud env command manifest access | One command repeatedly reads and parses the same near-16-MiB `lpm.json`. | A command-scoped manifest snapshot now serves all local manifest consumers while fresh pre-POST binding checks and guarded metadata commits preserve mutation safety. On the same 16,773,120-byte fixture, 50 alternating Release A/B pairs after five warmups reduced median time from 24.004 ms to 17.148 ms (28.56%) and median peak RSS from 83,271,680 to 67,903,488 bytes (18.46%). | Verified | Workflow read-count and manifest/principal replacement regressions; deterministic 50-pair Release time/RSS benchmark | Implementation commits above | Review in concept PRs |
| SEC-RESUME-001 | security_final | Security | CLI live/pending X25519 private keys, rotation lock, and public-key mutations across Registry authorities and authenticated principals | Key material and rotation state could be reused globally after switching Registry or account, and legacy unscoped slots could be adopted without proving that their public half matched the authenticated server binding. | The unscoped filenames, Keychain accounts, and lock name reproduced the shared trust domain. Slots and locks now use a digest of the canonical Registry URL and authenticated caller ID; legacy live and pending keys migrate only when their public half matches authenticated server state; public-key writes carry and re-confirm `expectedPrincipalId`; signed organization pulls carry `callerUserId` and select only that scoped key. A post-upload organization caller change discovered during verification also now fails closed. | Verified | Cross-Registry/caller slot isolation; same/different-scope lock tests; matching and mismatched legacy live/pending adoption; exact upload body and post-upload principal/caller substitution tests; signed organization pull caller-selection, substitution, missing, and malformed identity tests; Registry public-key and organization route suites; targeted CLI organization workflows | Implementation commits above | Review in concept PRs |

| C-R5-001 | round5_performance | Correctness | CLI file-backed vault persistence in `storage_transaction.rs` and `fallback.rs` | Encrypted/Base64 vault data can exceed the 16 MiB read cap even though the write succeeds, leaving the newly written vault unreadable. | A bug-first forced-file-backend test wrote a 16 MiB value successfully and then could not meet the required rejection contract. The write boundary now rejects encoded owner-only state above the same cap used by reads before atomic replacement, and the previous readable vault remains intact. | Verified | `file_backed_vault_rejects_an_encoded_value_above_the_read_limit_without_replacing_data`; oversized preservation workflow regression | Implementation commits above | Review in concept PRs |
| S-R5-001 | round5_security | Security | Swift X25519 key storage and organization sync/import caller binding | Swift used one global private-key account, ignored authenticated caller identity, and could diverge from the CLI's Registry-and-caller-scoped key after rotation. | Swift now derives the exact CLI-compatible account from the canonical Registry URL and authenticated caller, validates caller identity from member inventories and signed pulls before key lookup, and adopts a legacy key only when its public half matches authenticated server state. The complete 38-test security suite passed. | Verified | Cross-Registry/caller isolation and same-scope stability; CLI derivation vector; malformed caller rejection; matching/mismatched legacy adoption; organization pull/import caller-substitution tests | Implementation commits above | Review in concept PRs |
| P-R5-001 | round5_performance | Performance | CLI Keychain and file-fallback whole-vault serialization | Owned serialization wrappers cloned every environment, key, and value before allocating serialized output. | Borrowed serialization wrappers remove the full logical-vault clone. Three alternating Release A/B runs of a 32×1,024-key fixture reduced median serialization from 4.960 ms to 3.703 ms (25.35%) and median peak RSS from 101,974,016 to 87,212,032 bytes (14.48%). | Verified | Multi-environment wire round-trip tests; preserved Release before/after binaries and deterministic seven-sample benchmark | Implementation commits above | Review in concept PRs |
| P-R5-002 | round5_performance | Performance | Registry `SecretsTable` and `EnvDiffView` edit/compare rendering | Parent-level edits resorted and reconciled all rows while both tables retained every row in the DOM. | Value drafts now remain inside memoized rows, so typing does not rerender sibling keys; edit and compare tables expose 200-row windows, and environment tabs expose 100-item windows. The 1,000-variable and 1,000-environment fixtures prove the initial DOM bounds and the row-render counter proves row-local typing. | Verified | `keeps active value drafts local to their row`; encrypted save-payload semantic regression; 1,000-row edit/diff and 1,000-tab window tests | Implementation commits above | Review in concept PRs |
| P-R5-003 | round5_performance | Performance | CLI dotenv parsing | The parser first collected every line slice, making newline-dense input consume disproportionate pointer metadata. | Parsing now consumes `str::Lines` as a streaming state machine and carries only the active multiline value. Three alternating Release A/B runs on identical near-16-MiB inputs reduced newline-dense median time from 68.399 ms to 62.040 ms and RSS from 429,096,960 to 24,264,704 bytes; multiline median time fell from 72.468 ms to 47.017 ms and RSS from 848,674,816 to 93,028,352 bytes. | Verified | Parser compatibility/escape tests; preserved Release binaries; deterministic three-sample newline-dense and multiline benchmarks | Implementation commits above | Review in concept PRs |
| P-R5-004 | round5_performance | Performance | CLI environment output formatting | Sorting and formatting retained maps, line vectors, replacement chains, and copied JSON strings that scaled with the complete secret payload. | Formatting now sorts borrowed entries and writes directly into one pre-sized output buffer; JSON serializes borrowed strings. On an identical 32,768-key fixture across three alternating Release A/B runs, median latency improved by 11.97%-74.57% and median peak RSS by 17.80%-51.80% across shell, dotenv, JSON, Docker, and GitHub formats. | Verified | Exact per-format output and injection regressions; preserved Release binaries; five-sample A/B benchmarks for every format | Implementation commits above | Review in concept PRs |
| P-R5-005 | round5_performance | Performance | Registry dashboard env-project listings | Summary listings projected full schemas and normalized them repeatedly for two badges. | Dashboard queries now compute scalar environment-count and schema-presence projections in PostgreSQL, while API list projections remain metadata-only. This removes up to about 25 MiB of schema JSONB from a 100-row maximum page before transport/serialization overhead. Both personal and organization projections ran successfully against real PostgreSQL. | Verified | Dashboard projection SQL-contract tests; no-client-normalization component test; real PostgreSQL personal/organization projection validation | Implementation commits above | Review in concept PRs |
| P-R5-007 | round5_performance | Performance | Swift workspace snapshot construction | Snapshot derivation allocated a `Set` per key and duplicated single-environment sorting. | One accumulator per key now tracks its first value, count, and drift flag, and the single-environment snapshot reuses its sorted array through Swift copy-on-write. Three alternating Release A/B runs on the 32×6,000-key fixture reduced median time from 548.609 ms to 545.898 ms and peak RSS from 40,140,800 to 39,731,200 bytes. | Verified | Snapshot equality and single/multi-environment derivation tests; preserved Release test bundles and seven-sample A/B benchmark | Implementation commits above | Review in concept PRs |
| P-R5-008 | round5_performance | Performance | CLI file-fallback encryption/decryption | Base64 and formatted-envelope construction copied whole encrypted payloads, while decryption copied decoded ciphertext to append its tag. | Detached AES-GCM operations now encrypt/decrypt in place, delimiter parsing is allocation-free, and Base64 appends directly into one pre-sized string while preserving the legacy wire format. On identical 10,000,000-byte fixtures across three alternating Release A/B runs, encryption peak RSS fell 24.68% (54,181,888 to 40,812,544 bytes) and decryption peak RSS fell 49.37% (60,801,024 to 30,785,536 bytes), with median latency effectively unchanged. | Verified | Existing-format round trip and malformed-format tests; preserved Release binaries; five-sample encryption/decryption A/B benchmark | Implementation commits above | Review in concept PRs |
| P-R5-009 | round5_performance | Performance | SwiftUI vault content derivation | Default all-key display used `filter { true }`, copying the full visible-key array, and search repeatedly normalized keys. | Empty-query identity paths now reuse snapshot arrays, and normalized keys are cached once in the snapshot. Three alternating Release A/B runs of 100 iterations across matrix/environment and empty/search modes reduced median time from 286.021 ms to 275.767 ms and peak RSS from 28,229,632 to 27,967,488 bytes. | Verified | Derivation equality/default/search tests; preserved Release bundles and nine-sample A/B benchmark | Implementation commits above | Review in concept PRs |
| P-R5-010 | round5_performance | Performance | Swift add-environment persistence | UI preflight serialized the prospective vault before authoritative persistence serialized it again. | Size enforcement now occurs only at the authoritative transactional persistence boundary, which retains the exact limit and rollback behavior without duplicate serialization. Three alternating Release A/B runs of the near-cap add path reduced median latency from 0.240 ms to 0.112 ms (53.40%); the roughly 1% RSS difference was measurement noise. | Verified | Exact over-limit, cancellation, concurrency, and durable-commit regressions; preserved Release bundles and nine-sample A/B benchmark | Implementation commits above | Review in concept PRs |
| P-R5-011 | round5_performance | Performance | Swift cloud import/pull validation | Already validated decoded maps were copied and revalidated across import and persistence boundaries. | Decode now produces an explicit validated remote-map value, empty-local imports return its storage directly, and merge paths validate only untrusted local input rather than the remote map again. Three alternating Release A/B runs of a 32×2,000-key payload reduced median time from 33.420 ms to 30.310 ms (9.31%), with essentially unchanged peak RSS. | Verified | Invalid-name/format parity and merge-count tests; preserved Release bundles and seven-sample A/B benchmark | Implementation commits above | Review in concept PRs |
| P-R5-012 | round5_performance | Performance | Registry environment-tab label rendering | Each tab scanned all environment aliases, producing quadratic label lookup across the tab strip. | Project-config normalization now builds one non-enumerable canonical-environment display index, making each label lookup O(1), and the visible tab strip is bounded to 100 items. A 2,000-alias Proxy regression throws on any rescan, while the 1,000-environment component fixture proves the tab-window bound. | Verified | `uses the canonical display index without rescanning aliases`; 1,000-environment tab-window test; rename/add/fallback display tests | Implementation commits above | Review in concept PRs |
| C-R5-002 | round5_correctness | Correctness | CLI organization share preparation and Registry mutation commit | A reported lead claimed that a credential-file switch could prepare recipient access as caller A and commit the later push under caller B. | Rejected by a deterministic workflow race: `RegistryClient` retained caller A's captured session and bearer after the credential file changed, the POST retained A's `expectedCallerUserId`, and the Registry validates that precondition against the transaction-time bearer identity. | Rejected | `env_share_keeps_prepared_member_access_on_one_captured_session`; Registry matching, missing, and substituted caller-precondition tests | Not applicable | Not applicable |
| C-R5-003 | primary_verification | Correctness | Registry dashboard schema-summary SQL in `db/queries/vault.js` | The first scalar summary implementation called nonexistent PostgreSQL function `jsonb_object_length(jsonb)`, so real dashboard list queries failed despite mocked SQL-contract tests passing. | Real PostgreSQL execution reproduced the undefined-function error for both projections. The predicate now uses `EXISTS (SELECT 1 FROM jsonb_each(...))`; personal and organization projections both execute successfully. | Verified | `uses PostgreSQL-supported predicates for non-empty legacy schemas`; real PostgreSQL personal/organization projection exercise; 24 focused tests | Implementation commits above | Review in concept PRs |
| C-CONT-001 | primary_verification | Correctness | CLI cross-machine authenticated sync fixture in `tests/workflows/tests/cross_command_flows.rs` and `tests/workflows/tests/support/mock_registry.rs` | The cross-machine round-trip fixture used a stateful personal-sync mock without the authenticated principal required by the signed revision envelope, so it no longer exercised a valid post-pairing push/pull exchange. | The workflow failed against the strengthened authenticated response contract until the fixture supplied one stable principal to both machines and the stateful mock. The complete 13,930-test nextest run then passed this workflow. | Verified | `flow_env_push_pull_cross_machine_round_trip`; authenticated stateful-mock envelope coverage | Implementation commits above | Review in concept PRs |
| C-CONT-002 | primary_verification | Correctness | Rust `lpm.json` schema generator and the public schema copies in Registry and CLI docs | The public schema omitted the new sync principal bindings and authority checkpoints, so editor and downstream schema validation rejected valid authenticated sync metadata. | Regenerating the schema changed both public copies by the same 140-line contract addition. Explicit drift validation against the isolated docs worktree passed 4 of 4 tests, and the complete Registry and docs checks passed. | Verified | `lpm_json_schema_rejects_malformed_sync_authority_metadata`; `lpm_json_schema_accepts_valid_sync_authority_metadata`; public-copy drift guards | Implementation commits above | Review in concept PRs |
| R4-CORR-FINAL-01 | final_clean_correctness | Correctness | Registry OIDC exchange in `app/api/vault/oidc/route.js` and `lockOidcPolicyForIssuance` in `db/queries/vault.js` | A policy deleted or narrowed after authorization but before credential commit could still issue one five-minute vault-read token from the stale snapshot. | Credential issuance now rereads the complete authorization projection under `FOR SHARE` inside the token/audit transaction and compares a stable fingerprint. Missing or changed policies produce `oidc_policy_changed` and insert neither token nor success audit. | Verified | `rejects issuance when the authorized policy changed before commit`; unchanged, changed, and deleted policy lock tests in `vault-oidc-scope.test.js` | Implementation commits above | Review in concept PRs |
| C-CONT-003 | primary_verification | Correctness | Registry joined and pre-resolved organization authorization in `lib/vault/org-env-access.js` and its route/action consumers | The new query-reduction helpers could evaluate a stale soft-deleted organization row as authorized because the shared evaluator did not itself reject `org.deleted`. | A direct deleted-row regression reproduced authorization before the guard. `evaluateOrgEnvAccess` and resolved helpers now fail closed with `ORG_ENV_NOT_FOUND` before membership roles or billing state are exposed. | Verified | `rejects a deleted organization before membership or billing state is exposed`; focused route and dashboard authorization matrices | Implementation commits above | Review in concept PRs |
| PERF-CONT-001 | final_clean_performance | Performance | CLI recipient trust exact-match path in `crates/lpm-vault/src/sync/recipient_set.rs` | Stable accepted recipient sets cloned every binding, serialized the full proposal, and hashed it before discovering that stored trust already matched. | The stored set is now parsed and compared against borrowed prepared bindings first; proposal bytes and SHA-256 are lazy. Five alternating Release A/B process pairs on 10,000 recipients reduced the median of five exact checks from 24.355 ms to 7.115 ms and median peak RSS from 237,191,168 to 26,263,552 bytes. Preserved binaries: `/private/tmp/lpm-perf-cont-binaries/rust-before` and `rust-after`. | Verified | `matching_durable_recipient_set_skips_proposal_serialization`; deterministic 10,000-recipient Release time/RSS harness | Implementation commits above | Review in concept PRs |
| PERF-CONT-002 | final_clean_performance | Performance | CLI organization wrapping in `crates/lpm-vault/src/sync/org.rs` | Every wrapped-key payload cloned the recipient user ID and fingerprint even though prepared recipients outlived synchronous request serialization. | `WrappedMemberKey` now borrows both fields and owns only the generated wrap. Fifteen alternating Release A/B process pairs with 10,000 maximum-width recipients and real wrapping reduced median time from 308.099 ms to 300.818 ms and median peak RSS from 24,870,912 to 20,873,216 bytes. | Verified | Exact organization JSON request coverage; deterministic 10,000-recipient Release time/RSS harness with preserved Rust binaries | Implementation commits above | Review in concept PRs |
| PERF-CONT-003 | final_clean_performance | Performance | Swift workspace snapshot scheduling in `VaultStore.swift` and `VaultWorkspaceTypes.swift` | Cancelled snapshot builds did not observe cancellation and unstructured replacements could overlap, multiplying CPU and transient RAM during rapid project mutations or reloads. | Snapshot construction now checks cancellation within large traversals and a shared actor serializes builders. Fifteen alternating optimized A/B process pairs for an eight-update burst on a 32-by-6,000 fixture reduced median retired instructions from 3,114,996,918 to 989,858,525 and median peak RSS from 34,881,536 to 19,955,712 bytes; only the final build completed. | Verified | `superseded workspace snapshot builds stop without overlapping replacements`; deterministic burst benchmark with `/private/tmp/lpm-perf-cont-binaries/swift-before` and `swift-after` | Implementation commits above | Review in concept PRs |
| PERF-CONT-004 | final_clean_performance | Performance | Swift organization push preparation in `LPMVault/Sources/Services/SyncService.swift` | The request path built 1,024 generic dictionaries and 4,096 generic entries before encoding the final organization body. | A typed `OrganizationPushBody` is encoded directly and request preparation is generic over `Encodable`. Twenty alternating optimized A/B process pairs at the 1,024-member limit reduced median time for twenty encodes from 50.734 ms to 25.996 ms and median peak RSS from 11,624,448 to 11,059,200 bytes. | Verified | `organization push includes the immutable organization identity` exact-body equality; deterministic Release encoding/RSS harness with preserved Swift binaries | Implementation commits above | Review in concept PRs |
| PERF-CONT-005 | final_clean_performance | Performance | Registry organization mutations in `lib/vault/org-env-access.js` and `db/queries/vault.js` | The locked mutation path fetched membership and vault write state in two serial queries after locking the organization. | The organization-first lock order is preserved, then one indexed joined projection returns membership and write state. Query-count coverage requires exactly the lock plus one projection while retaining role, billing, slug-race, and lock-mode behavior. | Verified | `loads membership and vault write state in one query after locking the organization`; sync-transaction authorization matrix | Implementation commits above | Review in concept PRs |
| PERF-CONT-006 | final_clean_performance | Performance | Registry slug-scoped vault list, pull, write, audit, and member-key routes | Routes resolved an organization slug and then performed one or two serial membership or vault queries, adding avoidable database round trips. | Shared slug-keyed projections now join organization, caller membership, and optional vault state in one bounded query while downstream transactions retain race-safe reauthorization. | Verified | One-query slug organization/vault projection tests; full list, pull, write, audit, member-key, role, billing, and not-found matrices | Implementation commits above | Review in concept PRs |
| PERF-CONT-007 | final_clean_performance | Performance | Registry dashboard vault and OIDC authorization in `app/actions/vault.js` and `lib/vault/org-env-access.js` | Dashboard actions fetched ownership, then organization, then membership serially; policy deletion repeated the same waterfall. | Primary-key projections now return vault or policy ownership, organization billing state, and caller membership in one query. All personal/organization error distinctions and role rules remain enforced by the shared evaluator. | Verified | `projects dashboard save authorization and write state in one query`; `projects OIDC policy authorization and caller membership in one query`; dashboard action matrices | Implementation commits above | Review in concept PRs |
| PERF-CONT-008 | final_clean_performance | Performance | Registry `EnvDiffView` environment selectors | Each native selector rendered all environments, creating 2,000 option nodes for a 1,000-environment comparison despite row and tab windowing. | Both selectors now use the shared searchable combobox with a 100-option window that retains selected values and moves the window during keyboard navigation. | Verified | `bounds environment options while keeping late environments searchable`; `keeps keyboard navigation within the bounded option window` | Implementation commits above | Review in concept PRs |
| S-CONT-001 | fresh_security_1 | Security | CLI native data-key promotion and Doctor storage reporting in `crates/lpm-vault/src/fallback.rs` and `crates/lpm-cli/src/commands/doctor/storage.rs` | A successfully promoted native data key leaves the file-fallback password in `~/.lpm`, so an offline copy can still decrypt local vaults while Doctor reports native protection. | The corrected promotion regression failed because the fallback key remained after the verified native write. Promotion and native-key reuse now durably remove a matching fallback key; any residual hybrid state has an explicit Doctor warning instead of a native-protected pass. | Verified | `data_key_promotes_existing_file_key_and_removes_fallback_copy`; existing-blob decrypt compatibility; hybrid backend and Doctor warning regressions | Implementation commits above | Review in concept PRs |
| S-CONT-002 | fresh_security_1 | Security | Swift plaintext export replacement in `LPMVault/Sources/Utilities/SecureFileWriter.swift` | A temporary export can inherit a non-owner macOS ACL that survives `fchmod(0600)` and the final rename. | The production-writer regression and an independent filesystem probe both retained `group:everyone inherited allow read` before the fix. The writer now installs an empty extended ACL through the open descriptor before applying the requested POSIX mode and fails closed if that cannot be enforced. | Verified | `sensitive file exports remove inherited access-control entries`; owner-only permission/content regression | Implementation commits above | Review in concept PRs |
| PERF-POST-001 | final_clean_performance | Performance | Swift workspace reconciliation in `VaultStore.swift`, `VaultWorkspaceTypes.swift`, and synthesized `VaultProject` equality | A one-secret mutation structurally compares nested environment dictionaries while deciding which snapshot changed. | Snapshot reconciliation now compares a mutation-refreshed UUID identity, reducing unchanged-project comparison from nested secret traversal to O(1), and performs snapshot construction away from the main actor. | Verified | `one-project changes rebuild only one workspace snapshot`; `workspace snapshot identity changes after a nested secret mutation`; `workspace snapshot reconciliation does not block the main actor`; superseded-build coverage | Implementation commits above | Review in concept PRs |
| PERF-POST-002 | final_clean_performance | Performance | Registry `useCombobox` search derivation | The hook filters `filteredItems` into an `availableItems` array that no consumer reads. | Source tracing confirmed the array had no semantic consumer. Removing the filter eliminates one O(n) traversal and allocation per search while the bounded searchable-select keyboard and selection suites retain behavior. | Verified | Searchable-select bounded-option and keyboard-navigation component suites | Implementation commits above | Review in concept PRs |
| PERF-POST-003 | final_clean_performance | Performance | Registry dashboard list/layout/page authorization paths | Organization and personal dashboard renders repeat authorization and database reads across rendering layers. | Request-cached loaders now share the personal projection and organization metadata snapshot across layout and page consumers while independently invoked actions retain their own authorization. | Verified | `shares one personal metadata query across metadata, layout, and page`; `uses one metadata-only organization dashboard authorization query`; dashboard action matrices | Implementation commits above | Review in concept PRs |
| PERF-POST-004 | final_clean_performance | Performance | Registry organization env access dashboard | Access rendering can materialize and render up to 10,000 members and serialize raw public keys unused by the client. | Member access now uses stable 100-row cursor pages, strips raw public keys from the client DTO, and mounts only the current bounded page. A 10,000-member fixture returns one page plus an opaque cursor. | Verified | `keeps a 10,000-member organization fixture to one bounded result page`; `keeps only one bounded member page mounted while navigating`; DTO projection coverage | Implementation commits above | Review in concept PRs |
| PERF-POST-005 | final_clean_performance | Performance | Registry personal sync GET and OIDC CI pull | Independent entitlement, vault, and policy reads are awaited serially. | Personal sync now uses one bounded entitlement-and-vault projection, and CI pull uses one bounded entitlement, vault, and policy projection while preserving error precedence. | Verified | `combines personal entitlement and vault pulls in one bounded query`; `combines CI entitlement, vault, and policy state in one bounded query`; route response matrices | Implementation commits above | Review in concept PRs |
| PERF-POST-006 | final_clean_performance | Performance | Rust per-environment Keychain and file-fallback reads | Owned decoded wrappers deep-clone the selected secret map before the wrapper is dropped. | Both backends now remove and return the selected environment map, transferring its existing strings without cloning. Pointer-identity tests cover Keychain and fallback storage. | Verified | Keychain and fallback `selected_environment_transfers_owned_secret_values` | Implementation commits above | Review in concept PRs |
| PERF-POST-007 | final_clean_performance | Performance | CLI local env diff and copy | Diff decrypts the monolithic vault twice, while copy reads it twice and reads again inside its mutation transaction. | Local diff now reads all environments once, and copy performs the source lookup and destination mutation in one vault transaction without redundant decrypts. | Verified | `env_diff_local_vs_local_reports_added_removed_and_changed_keys`; local copy behavior and vault transaction coverage | Implementation commits above | Review in concept PRs |
| PERF-POST-008 | final_clean_performance | Performance | CLI `lpm env init` scan and commit | Dotenv files are parsed twice and each environment independently rewrites the growing monolithic vault, creating approximately quadratic persistence work. | Initialization now parses each file once, enforces an aggregate source budget, and applies the complete batch in one vault transaction while retaining force, skip, empty, and concurrent-update semantics. | Verified | `environment_inventory_and_initialization_decode_unchanged_vault_once`; `environment_initialization_refreshes_a_snapshot_after_a_concurrent_vault_write`; `environment_initialization_bounds_aggregate_source_bytes` | Implementation commits above | Review in concept PRs |
| PERF-POST-009 | final_clean_performance | Performance | Rust `lpm env export` dotenv formatter | Export allocates per-line strings, chained replacement copies, an owned sort vector, and a second joined output. | The formatter now sorts borrowed entries and escapes directly into one capacity-planned output buffer, eliminating per-line strings, chained replacements, and the joined intermediate. | Verified | Dotenv exact-output, control-whitespace, literal round-trip, and near-limit formatter coverage | Implementation commits above | Review in concept PRs |
| PERF-POST-010 | final_clean_performance | Performance | Rust macOS Keychain mutations | Existing-vault mutations unconditionally rewrite the unchanged `__index__` item. | Existing-vault writes now preserve the unchanged index item; only new vault membership marks the index as changed, retaining rollback behavior. | Verified | `cli_write_preserves_existing_vault_project_metadata`; `cli_write_marks_a_new_vault_index_entry_as_changed` | Implementation commits above | Review in concept PRs |
| PERF-POST-011 | final_clean_performance | Performance | CLI Vercel and GitHub Actions pagination and bounded response collection | Per-page limits permit excessive cumulative bytes, Vercel lacks cursor-cycle detection, and the collector ignores safe known content length. | Both integrations now share cumulative response budgets, GitHub bounds page and value counts, Vercel rejects repeated cursors, and the collector uses a safe known `Content-Length` for initial capacity. | Verified | `pagination_budget_accepts_bodies_at_the_exact_limit`; `pagination_budget_rejects_a_cumulative_overflow`; `known_response_length_sets_the_initial_capacity`; `vercel_repeated_pagination_cursor_fails_closed`; GitHub page/value limits | Implementation commits above | Review in concept PRs |
| PERF-POST-012 | final_clean_performance | Performance | Swift dotenv export and clipboard formatting | Multi-pass replacement, per-line allocation, joining, and String-to-Data conversion retain several plaintext-sized buffers. | A shared single-pass formatter writes escapes into one exactly reserved buffer; file export constructs `Data` directly while clipboard output reuses the same lossless semantics. | Verified | `Swift export round-trips Rust-compatible escapes`; `dotenv export and clipboard text remain literal when sourced`; `dotenv formatter and file-data profile` | Implementation commits above | Review in concept PRs |
| C-R6-001 | fresh_correctness_1 | Correctness | CLI personal sync payload decoding and pull commit | A wrapper-shaped authenticated plaintext without `environments` is converted to an empty replacement and can erase local secrets. | A strict payload enum now distinguishes the required versioned wrapper from the supported legacy flat map and rejects a wrapper without `environments` before local commit. | Verified | `overwrite_payload_without_environments_is_rejected`; `overwrite_payload_moves_the_owned_environment_map`; legacy-flat parser coverage | Implementation commits above | Review in concept PRs |
| C-R6-002 | fresh_correctness_1 | Correctness | CLI pull, push, share, and remote-list argument parsing | Malformed or unsupported organization selectors can fall through to personal scope, including an unprompted destructive pull. | Dedicated strict parsers now reject missing, empty, duplicate, joined-invalid, conflicting, and unknown arguments before dispatch instead of treating them as personal scope. | Verified | `parse_pull_target`, `parse_share_org_slug`, and `parse_list_remote_org_slug` matrices; `env_pull_joined_org_selector_never_falls_back_to_personal_scope` | Implementation commits above | Review in concept PRs |
| C-R6-003 | fresh_correctness_1 | Correctness | Registry protocol-v2 and legacy pairing approval CAS routes | A concurrent approval loser can be reported as HTTP 500 because the diagnostic lookup only searches the previous status. | Zero-row approval results now distinguish an expired session from a changed state and both protocol-v2 and legacy race losers map to stable HTTP 409 responses without success audit side effects. | Verified | `maps a lost protocol-v2 approval race to HTTP 409`; `maps a lost legacy approval race to HTTP 409`; `vault-pairing-cas.test.js` expiry diagnostics | Implementation commits above | Review in concept PRs |
| C-R6-004 | fresh_correctness_1 | Correctness | Registry OIDC remediation generation and CLI policy defaults | Generated remediation can target `production` instead of the request's implicit `default` environment and can omit the failing GitHub event. | Remediation now carries the request's effective environment and verified current event, while CLI policy creation uses the same default environment contract. | Verified | `remediates the implicit default environment and current safe GitHub event`; CLI OIDC remediation/default parsing coverage | Implementation commits above | Review in concept PRs |
| C-R6-005 | fresh_correctness_1 | Correctness | Organization env deletion and CLI bound-checkout recreation | A deleted organization env at revision greater than one cannot be recreated because the client retains the old checkpoint while creation requires revision one. | Rejected: resetting to revision one would permit replay below the durable Registry floor. The Registry retains the deletion floor and authenticated recreation advances monotonically from revision N to N+1; focused tests reproduced `5 → 6` for personal and organization scopes. | Rejected | `env_force_push_recreates_a_deleted_bound_vault_above_its_checkpoint`; personal and organization `recreated_*_vault_advances_only_the_matching_authority_checkpoint`; Registry recreation-floor route/query tests | Not applicable | Not applicable |
| C-R6-006 | fresh_correctness_1 | Correctness | Registry browser crypto decoding and dashboard pairing-key error handling | Malformed authenticated plaintext can pass weak shape checks or clear a valid pairing key as though decryption failed. | Browser decoding now requires valid UTF-8 and a strict versioned or legacy-flat payload shape with inert own properties. Dashboard code retains a valid pairing key for authenticated payload or per-vault corruption and clears it only for actual pairing-key failure. | Verified | Browser payload UTF-8, version, wrapper, and prototype-name matrix; `retains the pairing key when authenticated vault plaintext is invalid`; `retains the pairing key when one vault key or ciphertext is corrupt` | Implementation commits above | Review in concept PRs |
| C-CONT-004 | primary_verification | Correctness | Registry `EnvDiffView` rows and `SecretsTable` variable and environment windows | When filtered rows, variables, or environments shrink below the current window offset, the components can render an empty page until another interaction changes the offset. | Render-time bounded offsets now clamp to the last valid window immediately after input shrink, while navigation still advances by the configured row or tab window. | Verified | `moves to the previous visible window immediately after the rows shrink`; `moves to the previous visible variable window immediately after variables shrink`; `moves to the previous visible environment window immediately after environments shrink` | Implementation commits above | Review in concept PRs |
| PERF-POST-013 | primary_verification | Performance | Rust batched environment initialization in `crates/lpm-vault/src/lib.rs` | The optimized initialization path still decoded the same unchanged monolithic vault for inventory and again inside the batch transaction. | Inventory now returns a reusable snapshot and digest. The batch reuses it when storage is unchanged and refreshes only after a concurrent mutation, reducing the common path to one decode without losing update safety. | Verified | `environment_inventory_and_initialization_decode_unchanged_vault_once`; `environment_initialization_refreshes_a_snapshot_after_a_concurrent_vault_write` | Implementation commits above | Review in concept PRs |
| C-CONT-005 | primary_verification | Correctness | Registry durable vault revision floors and Rust recreation checkpoints | A proposed recreation reset to revision one would reopen accepted ciphertext revisions after deletion and undermine rollback protection. | Registry deletion retains an immutable floor; recreation requires the authenticated expected floor and stores floor plus one. Rust commits the same monotonic revision only for the matching Registry, principal, vault, and organization scope. | Verified | Registry personal and organization recreation-floor route/query tests; `env_force_push_recreates_a_deleted_bound_vault_above_its_checkpoint`; personal and organization `recreated_*` checkpoint tests | Implementation commits above | Review in concept PRs |
| C-CONT-006 | primary_verification | Correctness | Swift streaming dotenv reader and multiline parser | CRLF physical lines inside a quoted multiline assignment could be classified independently and dropped or retain a carriage return, diverging from the non-streaming parser. | The reader now preserves every physical line while a quoted assignment is pending and normalizes CRLF at the streaming boundary; the parser retains original source line numbers for diagnostics. | Verified | `streaming worker preserves physical line numbers and CRLF multiline values`; multiline size and compatibility regressions | Implementation commits above | Review in concept PRs |
| S-CONT-003 | primary_verification | Security | Registry authenticated sync request nonce admission and Swift/Rust response verification | Signed sync responses were not tied to the initiating request, so a valid same-scope response could be replayed into another in-flight operation before local postflight checks. | Clients now send a fresh 256-bit URL-safe nonce and require the signed envelope to echo it exactly. Registry routes validate the bounded nonce before ciphertext reads and include it in every signed success and conflict envelope. | Verified | Personal and organization invalid-nonce-before-query tests; Swift fresh/missing/replayed nonce response tests; Rust authenticated-envelope nonce matrix | Implementation commits above | Review in concept PRs |
| PERF-POST-014 | final_clean_performance | Performance | Registry `EnvDiffView` diff derivation | The component materializes a row object for every key plus filtered and missing row arrays even though it renders at most 200 rows. | Nine-sample deterministic profile: retained heap fell from 9,035,320 to 1,730,000 bytes, peak RSS from 116,352 to 105,376 KiB, and median derivation time from 18.752 to 17.996 ms. | Verified | Nine `EnvDiffView` regressions; bounded 200-row materialization; `benchmark-vault-env-diff.mjs` | Implementation commits above | Review in concept PRs |
| PERF-POST-015 | final_clean_performance | Performance | Rust organization remote merge in `crates/lpm-vault/src/lib.rs` | Organization pull commit deep-clones the complete latest vault before conflict validation. | Seven alternating isolated Release samples on the owned 12 MiB graph: median peak RSS fell from 45,203,456 to 32,538,624 bytes and median wall time from 9.624 to 8.850 ms; pointer tests prove allocation transfer. | Verified | Conflict/rollback regressions, owned-allocation transfer coverage, preserved A/B binaries and profile output | Implementation commits above | Review in concept PRs |
| PERF-POST-016 | final_clean_performance | Performance | Rust dotenv imports in `crates/lpm-vault/src/lib.rs` | Both individual import paths clone every parsed key and value into the target vault. | Seven alternating isolated Release samples on 256 × 48 KiB values: median peak RSS fell from 32,522,240 to 19,873,792 bytes and median wall time from 8.101 to 7.474 ms; pointer identity proves ownership transfer. | Verified | Import overwrite/skip regressions, pointer-identity coverage, preserved A/B binaries and profile output | Implementation commits above | Review in concept PRs |
| PERF-POST-017 | final_clean_performance | Performance | Runtime vault loading in `crates/lpm-runner/src/dotenv.rs` | Missing or empty requested environments can read, decrypt, and deserialize the same monolithic vault twice. | Instrumented regressions reproduce two complete decodes before the change and exactly one validating selective decode after it for both absent and empty requested environments. | Verified | `try_get_all_env_with_default_fallback` compatibility coverage and absent/empty single-decode-count regression | Implementation commits above | Review in concept PRs |
| PERF-POST-018 | final_clean_performance | Performance | Rust maintainer organization push in `crates/lpm-vault/src/sync/org.rs` | Maintainer push decrypts and retains the old full plaintext even though the replacement only needs the content key and authenticated metadata. | Authenticated unusable ciphertext succeeds only after the fix. Seven alternating isolated Release samples on a valid 15 MiB old payload reduced median peak RSS from 43,941,888 to 28,082,176 bytes and median wall time from 83.330 to 9.675 ms. | Verified | Corrupt-old-ciphertext replacement regression plus preserved 15 MiB decrypt/no-decrypt A/B fixture and binaries | Implementation commits above | Review in concept PRs |
| PERF-POST-019 | final_clean_performance | Performance | Rust fallback snapshot digest in `crates/lpm-vault/src/fallback.rs` | Snapshot reuse allocates the full encrypted vault again solely to compute SHA-256 while the decoded snapshot remains live. | Seven alternating isolated Release samples on a 15 MiB owner-only file: median peak RSS fell from 25,231,360 to 7,421,952 bytes and median wall time from 34.999 to 30.690 ms. | Verified | Streaming SHA-256 parity and sparse-file cap tests; preserved A/B binaries and profile output | Implementation commits above | Review in concept PRs |
| PERF-POST-020 | final_clean_performance | Performance | Rust targeted environment reads in Keychain, fallback, and personal sync | Selecting one environment deserializes and retains every environment map before removing the requested map. | Seven alternating isolated Release samples on a 14 MiB multi-environment payload: median peak RSS fell from 36,864,000 to 21,676,032 bytes and median wall time from 9.092 to 7.864 ms. | Verified | Versioned/legacy compatibility matrix, malformed-unselected validation, allocation-transfer coverage, preserved A/B profile | Implementation commits above | Review in concept PRs |
| PERF-POST-021 | final_clean_performance | Performance | Rust file-fallback data-key derivation in `crates/lpm-vault/src/fallback.rs` | Every fallback read and write performs an approximately 1 GiB scrypt derivation despite the stored password already being uniformly random and colocated with its salt. | Seven alternating isolated production-strength Release samples: median reuse time fell from 10,415.899 to 17.082 ms and median peak RSS from 1,081,278,464 to 7,503,872 bytes. Legacy material still derives once and migrates atomically. | Verified | Direct-key creation, one-time migration, persistence-failure, malformed-version and no-rewrite tests; preserved A/B binaries | Implementation commits above | Review in concept PRs |
| PERF-POST-022 | final_clean_performance | Performance | Registry browser plaintext normalization in `lib/vault/browser-crypto.js` | Strict parsing originally copied the complete parsed object graph into new null-prototype maps. | Duplicate companion to C-R6-006. Nine isolated samples show median normalization time falling from 50.128 to 0.081 ms, retained heap from 28,509,824 to 23,787,880 bytes, and peak RSS from 141,872 to 102,576 KiB. | Verified | 26 strict browser-crypto regressions and `benchmark-vault-browser-normalization.mjs` | Implementation commits above | Review in concept PRs |
| PERF-POST-023 | final_clean_performance | Performance | Rust quoted dotenv parsing in `crates/lpm-vault/src/lib.rs` | Double-quoted values are accumulated into one full string and then copied into a second unescaped string with no initial capacity. | Seven alternating isolated Release samples on a 14 MiB escaped value: median peak RSS fell from 46,055,424 to 29,245,440 bytes. Median time was within harness noise (28.279 vs 29.105 ms) while eliminating the full intermediate buffer. | Verified | Exact escape/multiline compatibility matrix and preserved A/B binaries and profile output | Implementation commits above | Review in concept PRs |

| C-FINAL-006 | final_zero_correctness | Correctness | Swift VaultPersistenceCoordinator.commitPullUnlocked environment-level conflict detection | Conflict checks visited only keys present in the remote payload, so a locally deleted empty baseline environment could be silently resurrected by a pull. | The commit now compares structural presence for every remote environment that existed in the baseline and rejects a locally removed environment before persistence. | Verified | pull rejects resurrection of an empty environment deleted locally | Implementation commits above | Review in concept PRs |
| C-FINAL-007 | final_zero_correctness | Correctness | Swift VaultStore.approveAndContinueOrgPush approval activation | Concurrent duplicate sheet activations could both pass the pre-await checks and invalidate each other's generation, leaving no organization push completed. | Approval activation now atomically consumes pendingOrgPush, closes the sheet, and claims one new sync generation before the first suspension; later activations observe no pending operation and return. | Verified | duplicate organization approval activation cannot cancel the claimed push | Implementation commits above | Review in concept PRs |
| C-FINAL-008 | final_zero_correctness | Correctness | Swift remote env-project import naming | Server-listed project names bypassed the canonical project-name contract and could persist C0/C1 controls or names longer than 200 UTF-16 code units. | Imported listed names and generated fallbacks now pass through EnvValidation.normalizedProjectName; invalid names fail before local persistence. | Verified | cloud imports reject project names outside the canonical contract | Implementation commits above | Review in concept PRs |
| C-FINAL-009 | final_zero_correctness | Correctness | Swift path-bearing VaultStore.addProject API | The dormant helper accepts a project path and was reported as a possible path-confusion surface. | Rejected: repository-wide caller tracing finds no production caller; the only production creation/import path is the separately validated, pathless addProjectWithVaultId, while addProject is exercised only by tests. | Rejected | repository search for addProject calls returns only the declaration; production call graph inspection | Not applicable | Not applicable |
| C-FINAL-010 | final_zero_correctness | Correctness | Swift paginated cloud and organization project listings | Duplicate vaultId values within or across pages violated SwiftUI row identity and made import selection ambiguous. | SyncService.listProjects now maintains one bounded cross-page identity set and fails with invalidPagination on the first duplicate before exposing the list. | Verified | cloud env project listing rejects duplicate project identities across pages; same-page duplicate is covered by the same insertion predicate | Implementation commits above | Review in concept PRs |
| C-FINAL-011 | final_zero_correctness | Correctness | Swift secret editor save activation in `VaultInspectorView` and `VaultSecretEditDraft` | Return and button activation could start duplicate saves before either asynchronous mutation completed, causing redundant persistence or a false compare-and-swap conflict. | `beginSave` now claims one in-flight save synchronously and rejects later activations until `finishSave`; the store-level regression persists exactly once. | Verified | `secret editor admits only one save activation until durable completion`; `duplicate editor save activation persists once without a false conflict` | Implementation commits above | Review in concept PRs |
| C-FINAL-012 | final_zero_correctness | Correctness | Swift `VaultStore.approveAndContinueOrgPush` | A transient authentication failure after consuming organization key approval silently ended the workflow without an actionable error. | The failure path now publishes a retryable authorization error after approval consumption instead of returning with no status. | Verified | `transient auth failure after organization approval is actionable` | Implementation commits above | Review in concept PRs |
| C-FINAL-013 | final_zero_correctness | Correctness | Swift current-user decoding and organization routing | Unbounded, duplicate, empty, or unsafe identity fields could replace coherent account state and later drive token inventory or organization routing. | `LPMUser.hasValidRoutingIdentity` now validates bounded user and organization identifiers, unique IDs and slugs, portable slugs, and the 256-organization ceiling before publication. | Verified | `oversized organization inventory is rejected before token requests`; `malformed identity responses do not replace a coherent account` | Implementation commits above | Review in concept PRs |
| C-FINAL-014 | final_zero_correctness | Correctness | Swift personal and organization token-revocation executor failure paths | A delayed failure from a superseded revocation could overwrite the error state of a new login or account environment. | Both catch paths now require the captured authorization generation and environment to remain current before publishing the error. | Verified | `superseded revocation executor failures do not overwrite the current context` | Implementation commits above | Review in concept PRs |
| C-FINAL-015 | final_zero_correctness | Correctness | Swift secret editor compare-and-swap baseline | The asynchronous save path could read its expected value after an external update instead of using the baseline displayed when the user submitted the edit. | `VaultInspectorView` captures the displayed baseline before claiming the save and passes it explicitly through `updateSecretAndWait`. | Verified | `editor save remains bound to the submitted baseline`; existing secret compare-and-swap regressions | Implementation commits above | Review in concept PRs |
| C-FINAL-016 | final_zero_correctness | Correctness | Swift project rename views and `VaultStore.renameProject` | The UI accepted names that the store later rejected, causing the sheet to close without applying the rename. | Both rename views and the store now share `VaultProjectRenamePolicy`, which delegates to the canonical project-name normalizer. | Verified | `project rename controls use the canonical name contract` | Implementation commits above | Review in concept PRs |
| C-FINAL-017 | final_zero_correctness | Correctness | Swift `VaultSecretEditDraft` delayed save completion | A successful completion for submitted value C could erase a newer external baseline received while that save was in flight. | Completion is now bound to `pendingSaveBaseline`; external updates remain authoritative, including coalesced C-to-success and edits made after submission. | Verified | `delayed save completion preserves newer external state`; secret draft conflict matrix | Implementation commits above | Review in concept PRs |
| C-FINAL-018 | final_zero_correctness | Correctness | Swift fixed-organization `OrgVaultsSheet` loading and import routing | If the parent changed the fixed organization from A to B, an in-flight or retained sheet state could still display or import organization A. | Load identity now includes the fixed slug, changes cancel stale work, and all selection/import routing uses the synchronized effective slug. | Verified | `fixed organization changes invalidate sheet loading and routing` | Implementation commits above | Review in concept PRs |
| C-FINAL-019 | final_zero_correctness | Correctness | Swift export task cleanup in `VaultDetailView` and `VaultWorkspaceView` | A canceled export A could run its deferred cleanup after replacement export B started and clear B's task slot, allowing overlapping work and misattributed errors. | Both views assign each export a UUID and only the owning request may clear its task/ID or publish a contextual error. | Verified | `stale export completion cannot release a replacement task`; admitted-export cancellation and navigation regressions | Implementation commits above | Review in concept PRs |
| C-GATE-001 | primary_verification | Correctness | Swift `VaultWorkspaceView.renameProject` | Optional-binding shorthand shadowed the `projectToRename` state property, so assigning `nil` produced a compile error. | `swift test` reproduced the compiler failure; binding to `project` preserves access to the state property for cleanup. | Verified | Complete Swift package compilation and test suite | Implementation commits above | Review in concept PRs |
| C-GATE-002 | primary_verification | Correctness | Swift protocol-v2 organization import regression fixture | The fixture omitted `callerUserId`, so account-binding rejection occurred before the intended unsupported-version assertion and the test did not cover its claimed path. | Adding the authenticated caller binding makes the request pass identity validation and reach the protocol-version rejection before decryption. | Verified | `network protocol v2 imports are rejected before decryption` | Implementation commits above | Review in concept PRs |
| PERF-GATE-001 | primary_verification | Performance | Registry `EnvDiffView` reveal reset effect | Reveal cleanup depended on an unstable callback identity, causing unnecessary effect executions and failing exhaustive-dependency analysis. | The effect now depends only on the derived disabled-state booleans and clears reveals only when either selected environment becomes disabled. | Verified | 11 focused `EnvDiffView` tests; Registry Biome lint | Implementation commits above | Review in concept PRs |
| C-GATE-003 | primary_verification | Correctness | CLI documentation for forced organization-vault recreation | The documentation said a recreated vault starts at revision 1, contradicting the durable rollback floor and the implemented checkpoint-plus-one contract. | Registry creation uses `revisionFloor.version + 1`; route and workflow regressions prove that a stored revision 5 recreates at revision 6. | Verified | `env_force_push_recreates_a_deleted_bound_vault_above_its_checkpoint`; personal and organization Registry recreation-floor tests | Implementation commits above | Review in concept PRs |
| SEC-DOC-001 | security_audit_final | Security | CLI docs `content/docs/reference/lpm-json.mdx` and `content/docs/infra/secrets-vault.mdx` | The documented `vaultSync` shape omitted the authoritative account and revision bindings and described legacy summary versions as the conflict-detection authority, so manual reconstruction could erase local rollback and cross-account substitution protection. | Runtime tracing confirmed `authorityCheckpoints` supplies the principal-scoped revision floor and `personalPlatformBindings` pins personal platform credentials, while the published schema already exposes both fields. The docs now distinguish authoritative bindings from non-authoritative summaries and warn against removing them. | Verified | `vaultSync documentation documents the authoritative account and revision bindings`; full docs check | Implementation commits above | Review in concept PRs |
## Local integration continuation

All values and accounts used in real local checks were disposable test fixtures.
The signed Developer ID app and CLI used the shared macOS Keychain, a local registry, and local Supabase.

| ID | Source | Category | Location | Claim | Evidence | Disposition | Coverage | Commit | PR status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| E2E-001 | primary local validation | Correctness | Registry pairing fixture | The happy-path fixture omitted the current principal and session binding. | Full unit suite returned 400 instead of 200; corrected fixture reaches the successful approval path. | Verified | pair-routes-step-up.test.js | e1c668be | Review in concept PRs |
| E2E-002 | primary local validation | Correctness | Registry startup and response signing | Missing response-signing configuration allowed a cloud write to commit before its response failed. | A signed CLI push committed but returned an unsigned 500. Startup validation now rejects missing or invalid signing configuration. | Verified | response-signature.test.js; lib/env/validate.test.js; real signed push | e1c668be | Review in concept PRs |
| E2E-003 | primary local validation | Correctness | Registry, Swift and CLI local response signing | The real clients had no supported signing configuration for local development. | Both clients rejected the published development fixture before the fix. Debug loopback HTTP responses now accept it; release and remote trust rules remain enforced. | Verified | Swift signature tests; CLI signing-policy matrix and platform workflow; actual signed round trips | d5590d7 / 05f3270e / e1c668be | Review in concept PRs |
| E2E-004 | primary local validation | Correctness | Swift signed-out account settings | The signed-out view hid the local-server selector. | A rendered OCR regression failed before the shared server controls were shown. Actual local sign-in passed. | Verified | CurrentContractTests.swift rendered settings regression | d5590d7 | Review in concept PRs |
| E2E-005 | primary local validation | Correctness | Swift conflict confirmation | The conflict dialog omitted the merge precedence and force-push consequences. | A rendered OCR regression failed before explanatory text was added. Actual Pull & Merge recovery passed. | Verified | CurrentContractTests.swift rendered conflict regression | d5590d7 | Review in concept PRs |
| E2E-006 | primary local validation | Correctness | Registry relational authorization SQL | Drizzle rebound inner table column references to the outer relational-query alias. | Actual dashboard request failed on a nonexistent outer column. Three compiled relational-query regressions failed before explicit inner identifiers fixed the query. | Verified | vault-dashboard-authorization.test.js; real personal and organization dashboard pages | e1c668be | Review in concept PRs |
| E2E-007 | primary local validation | Correctness | Registry organization array bindings | Drizzle expanded recipient arrays into scalar SQL parameters. | Actual organization share failed. Six compiled-SQL regressions failed before explicit array parameters fixed one- and two-recipient writes. | Verified | vault-array-bindings.test.js; actual recipient approval and share | e1c668be | Review in concept PRs |
| E2E-008 | primary local validation | Correctness | Swift existing-project organization import | Import reported Added while the organization sidebar hid the existing personal project. | The store regression returned duplicate before the fix. Move here now offers an explicit atomic merge, preserves local state and checkpoints, and rolls back failed persistence. | Verified | OrganizationProjectImportTests.swift; VaultStoreTests.swift; actual native move and share | d5590d7 | Review in concept PRs |
| E2E-009 | primary local validation | Correctness | Registry organization Activity page | The page treated the new audit pagination envelope as an array. | Both page regressions failed before reading entries and nextCursor from the envelope. The real browser now renders activity. | Verified | org-vault-activity.test.jsx | e1c668be | Review in concept PRs |
| E2E-010 | primary local validation | Correctness | Registry sharing-key onboarding | The access page incorrectly claimed that login registers the sharing key. | Copy assertions failed before documenting organization pull, account verification and owner re-share. The actual member onboarding followed that path. | Verified | VaultAccessControl.test.jsx; real maintainer onboarding | e1c668be | Review in concept PRs |
| E2E-011 | primary local validation | Correctness | Registry membership cleanup audit SQL | JSON audit parameters had no PostgreSQL types. | Actual leave failed atomically with PostgreSQL 42P18. A compiled-SQL regression failed before uuid and text casts were added; leave then completed with wrapped-key cleanup and required rotation. | Verified | vault-array-bindings.test.js; actual leave, denied pull and owner rotation | e1c668be | Review in concept PRs |
| E2E-012 | primary local validation | Correctness | CLI cross-machine workflow fixture | The fixture seeded a standalone bearer and returned an obsolete write action. | The full workspace gate failed before cloud sync with not logged in. A refresh-backed fixture and current signed responses now complete the two-HOME encryption round trip. | Verified | cross_command_flows::flow_env_push_pull_cross_machine_round_trip | 05f3270e | Review in concept PRs |
| E2E-013 | primary local validation | Correctness | CLI environment inventory iteration | The inherited helper invoked into_iter on a borrowed map. | The full workspace Clippy gate failed. Explicit iter passes with zero warnings. | Verified | Workspace Clippy --all-targets -- -D warnings | 05f3270e | Review in concept PRs |
| E2E-014 | primary local validation | Correctness | CLI GitHub Actions oversized-value fixture | The test omitted the concurrent successful secret-list response. | The targeted test could report the missing list response instead of the intended value-size error. The complete mock response and diagnostic assertion pass. | Verified | github_actions platform tests | 05f3270e | Review in concept PRs |
| E2E-015 | primary local validation | Correctness | Registry local database bootstrap | The local fixture changed historical migration files and omitted the legacy manual search-vector setup. | Original-history validation now installs the existing db/sql/0002 and 0005 setup before migration 0158, uses the required Supabase extensions, and preserves exact migration hashes. On a separate fresh database, db:push applied 0169–0177 and db:audit-migrations reported zero pending migrations, zero historical gaps and zero schema issues. The earlier claim that the function definitions were absent was disproved by those SQL files. | Verified | Original-history baseline replay followed by db:push and db:audit-migrations; no edits to applied historical files | Validation evidence and DEVELOPMENT.md | Review in concept PRs |

| E2E-016 | primary CI validation | Correctness | CLI sharing-key account helper compilation | macOS-only account helpers were compiled into non-macOS production libraries without callers. | The current-head Linux Clippy job failed with dead-code errors before the fix. The helpers now compile only on macOS or in unit tests, where both live and pending names are checked against the Swift contract. | Verified | Existing Linux workspace Clippy gate; sharing_key_rotation_account_matches_the_swift_client; local workspace Clippy | 45f6f36b | Review in concept PRs |

### Real client evidence

- Personal CLI → app → CLI sync passed in default and production environments.
- Native conflict recovery, quoted export with mode 0600, and multiline import passed.
- Browser pairing and browser edits round-tripped through both signed clients.
- The native app moved an existing project into its organization and shared an edited value.
- A maintainer registered a sharing key, received access after owner re-share, pulled, edited, and shared.
- Leaving the organization removed membership and the wrapped key, wrote the membership audit event, and required key rotation.
- The departed member's pull was denied. Owner rotation produced revision 6 and content-key version 5.
- The native app authenticated a network pull of revision 6 and displayed the maintainer's exact saved value.
- Database inspection confirmed that only the owner retained a wrapped key at content-key version 5 and that rotation was no longer required.

### Verification scope

The Swift suite passed 593 tests and both signed macOS builds passed.
The CLI fast gates passed 6,234 library/integration tests, 5,087 serial CLI unit tests, and 97 CLI binary-surface tests.
The affected workflow targets passed 783 tests before the additional full-workspace check.
The final current-head full-workspace run passed all 14,053 tests, with 21 ignored tests.
The separate audit profile passed its Next.js installation fixture, with no skipped tests.
Workspace build, Clippy, and formatting passed on Rust 1.94.0 with zero warnings.
An earlier unchanged Bun converter test returned macOS EPERM once. It then passed 30 exact reruns and both later full-workspace runs.
A diagnostic rerun also exposed an incorrect schema-directory environment setting; the final run used the correct public/schemas directory.
The updated registry branch passed 7,312 unit tests, lint, revalidate-tag checks, and its production build.
The updated docs branch passed 78 unit tests, 15 component tests, types, lint, content-date validation and its production build.
Current-head CI results are available in the linked pull requests. The Swift and documentation repositories have no configured PR checks; their complete local suites passed.

Platform adapters have mock workflow coverage. External Stripe, Cloudflare, R2 and Worker deployments were not exercised.
The vendor acceptance harness requires its external development environment and was not bypassed.
Organization browser decryption and organization OIDC access are unavailable in the current contract.
Historical bootstrap edits and unrelated AI-test timeout changes remain outside the published concept.

## Cumulative totals

- Findings received: 172 (156 inherited and 16 local and CI validation findings).
- Findings verified and fixed: 168.
- Findings rejected with evidence: 4.
- Findings externally blocked: 0.
- Findings pending: 0.
