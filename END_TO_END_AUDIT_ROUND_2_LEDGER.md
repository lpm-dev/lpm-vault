# End-to-end audit round 2 ledger

Concept branch: `codex/end-to-end-audit-round-2`  
Base commit: `6751d691138e0dddc8a3eb2fa648055c60e8e43e`  
Implementation commit: `76110e8`  
Concept pull request: [#16](https://github.com/lpm-dev/lpm-vault/pull/16)

This ledger covers the correctness, security, and performance audit after pull request #15.

The three read-only subagents supplied audit leads. The primary agent reproduced or disproved each lead before its final disposition.

| ID | Source | Category | Location | Claim | Evidence | Disposition | Coverage | Commit | PR status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| CORR-001 | correctness_audit | Correctness | `AuthSessionCoordinator` refresh commit and authority reads | A failed access-token write can strand a durable rotated refresh token behind a mismatched authority record. | The base regression found the rotated refresh token and a stale access authority. The optimized path treats the cleanup marker as a recovery state. | Verified | `a refresh-only partial peer commit is recovered without using the predecessor` | `76110e8` | Included in [#16](https://github.com/lpm-dev/lpm-vault/pull/16) |
| CORR-002 | correctness_audit | Correctness | `AuthSessionCoordinator.deviceFingerprint` | A storage failure returned a new random device identity for each request. | An injected storage failure reproduced transient identities. The coordinator now reports the failure and verifies a new identifier after the write. | Verified | `device identity storage failures are explicit and never mint transient identities` and concurrent initialization coverage | `76110e8` | Included in [#16](https://github.com/lpm-dev/lpm-vault/pull/16) |
| CORR-003 | correctness_audit | Correctness and security | `VaultStore.addProject` completion after `lock()` | Folder re-add can publish decrypted Keychain data after the vault locks. | A production call-site search found no caller of `addProject`. The active creation path uses `addProjectWithVaultId` with empty environments. | Rejected | Production call-site inventory and existing creation tests | Not applicable | Rejected in [#16](https://github.com/lpm-dev/lpm-vault/pull/16) |
| CORR-004 | correctness_audit | Correctness | Sidebar removal transaction | The UI removed a project before the protected index update and ignored persistence failures. | An injected index failure hid the project on the base path. The UI now changes only after the index update succeeds. | Verified | `sidebar removal publishes only after the index update succeeds` and import-race coverage | `76110e8` | Included in [#16](https://github.com/lpm-dev/lpm-vault/pull/16) |
| CORR-005 | correctness_audit | Correctness | `deleteProjectAndMetadata` final project list | A read failure after a deletion commit reported a false failure and left stale UI state. | The base regression deleted the project and then failed the final read. The transaction now derives the final list from its complete pre-commit snapshot. | Verified | `a read failure after the deletion commit cannot report a false failure` | `76110e8` | Included in [#16](https://github.com/lpm-dev/lpm-vault/pull/16) |
| CORR-006 | correctness_audit | Correctness and security | Keychain environment read and write boundaries | Decodable invalid maps and wholly empty wrappers bypassed environment validation. | Real-Keychain tests accepted invalid names and empty wrappers on the base path. Reads and writes now validate and normalize the maps. | Verified | `invalid decoded environment maps fail closed`, invalid-write coverage, and empty-wrapper normalization | `76110e8` | Included in [#16](https://github.com/lpm-dev/lpm-vault/pull/16) |
| CORR-007 | correctness_audit | Correctness | `VaultStore.loadIdentity` terminal authorization errors | A terminal unauthorized response retained a stale user and token inventory. | The base regression kept the prior identity. The terminal branch now clears the user and token state. Transient failures preserve coherent state. | Verified | `an unauthorized identity response clears stale authenticated state` | `76110e8` | Included in [#16](https://github.com/lpm-dev/lpm-vault/pull/16) |
| CORR-008 | correctness_audit | Correctness | Organization trust reads, writes, and approval continuation | Trust-store errors appeared as empty trust or success. Approval continued before trust was durable. | Injected read and write failures reproduced both paths. Trust now uses the protected service, and the push starts only after a successful trust write. | Verified | `organization trust read failures stop sharing instead of appearing untrusted` and `organization approval never pushes before trust is durable` | `76110e8` | Included in [#16](https://github.com/lpm-dev/lpm-vault/pull/16) |
| CORR-009 | correctness_audit | Correctness | Organization project creation | Separate project and association writes left a personal project after an association failure. | An injected association failure reproduced the orphan project. One protected transaction now creates both records or restores both snapshots. | Verified | `organization vault creation rolls back if association persistence fails` and association rollback coverage | `76110e8` | Included in [#16](https://github.com/lpm-dev/lpm-vault/pull/16) |
| CORR-010 | correctness_audit | Correctness | Folder `lpm.json` read and write linking | A malformed or unwritable project file can fail after a Keychain commit. | The folder-link functions have no production caller. Current UI creation and cloud import paths use an empty project path. | Rejected | Production call-site inventory and existing folder helper tests | Not applicable | Rejected in [#16](https://github.com/lpm-dev/lpm-vault/pull/16) |
| CORR-LC-001 / SEC-AUTH-001 | correctness_audit and security_audit | Correctness and security | `BiometricService` cache clock | A wall-clock rollback extended cached authentication for a sensitive action. | A negative elapsed value passed the base cache condition. The cache now uses uptime and rejects negative elapsed values. | Verified | `biometric cache does not survive a clock rollback` | `76110e8` | Included in [#16](https://github.com/lpm-dev/lpm-vault/pull/16) |
| CORR-LC-002 | correctness_audit | Correctness | Login attempt limiter | A wall-clock rollback blocked login attempts until the clock caught up. | A negative elapsed value triggered the base five-second limit. The limiter now uses uptime and reserves a new attempt after an uptime reset. | Verified | `login attempt throttling does not persist across a monotonic clock reset` | `76110e8` | Included in [#16](https://github.com/lpm-dev/lpm-vault/pull/16) |
| CORR-LC-003 | correctness_audit | Correctness | `UpdateChecker` cache age | A future cache timestamp stayed valid until local time caught up. | The base age calculation treated a negative age as fresh. The cache now expires negative ages. | Verified | `future-dated update cache entries expire immediately` | `76110e8` | Included in [#16](https://github.com/lpm-dev/lpm-vault/pull/16) |
| CORR-LC-004 | correctness_audit | Correctness | Cloud and organization relative timestamps | A future server timestamp displays `just now`. | This display is the defined clock-skew policy. The shared formatter clamps future values to zero and keeps the same UI text. | Rejected | `shared RFC 3339 relative timestamps cover fractional, whole, and future values` | Not applicable | Rejected in [#16](https://github.com/lpm-dev/lpm-vault/pull/16) |
| CORR-LC-005 | correctness_audit | Correctness | Remote push followed by local metadata failure | Server success with a local metadata failure leaves a stale expected version. | The app reports this state as a partial failure. The stale version forces a server conflict and a safe pull on the next operation. | Rejected | Source trace through `finishPush` and conflict recovery tests | Not applicable | Rejected in [#16](https://github.com/lpm-dev/lpm-vault/pull/16) |
| SEC-SYNC-002 | security_audit | Security and correctness | `SyncService.request` response status | Unsigned non-2xx JSON with a success shape passed to push and pull decoders. | A base 409 response decoded as success. The shared request path now requires a 2xx status before body decoding. | Verified | `sync push and pull reject unsigned non-success responses with success-shaped JSON` | `76110e8` | Included in [#16](https://github.com/lpm-dev/lpm-vault/pull/16) |
| SEC-SYNC-001B | security_audit | Security | Sync protocol v1 AEAD context | Protocol v1 ciphertext does not authenticate the vault or scope identity. | Protocol analysis reconfirmed the missing associated data. A safe local migration requires coordinated server and CLI support. | Blocked | Migration vectors in [issue #13](https://github.com/lpm-dev/lpm-vault/issues/13) | Server and CLI protocol migration | Tracked by [#13](https://github.com/lpm-dev/lpm-vault/issues/13) |
| SEC-KEY-002 | security_audit | Security | Legacy wrapping-key file cutover | A filesystem key copy remains after Keychain migration. | Supported CLI versions still require this file. App-only deletion breaks compatibility and risks data loss. | Blocked | Cutover coverage in [issue #12](https://github.com/lpm-dev/lpm-vault/issues/12) | Compatible CLI release | Tracked by [#12](https://github.com/lpm-dev/lpm-vault/issues/12) |
| SEC-KEY-003 | security_audit | Security | Legacy Keychain compatibility authority | Legacy Keychain data stays authoritative before a coordinated cutover. | Current CLI versions still write legacy data. A local authority change can hide valid CLI updates. | Blocked | Cutover and rollback coverage in [issue #12](https://github.com/lpm-dev/lpm-vault/issues/12) | Compatible CLI and app contract | Tracked by [#12](https://github.com/lpm-dev/lpm-vault/issues/12) |
| PERF-CACHE-001 | performance_audit | Performance | `projects.didSet` workspace cache | A one-project change rebuilt every workspace snapshot on the main actor. | The 100-project fixture dropped from 121.457–127.607 ms to 2.374–2.594 ms. One changed project now causes one rebuild. | Verified | `one-project changes rebuild only one workspace snapshot` and A/B benchmark | `76110e8` | Included in [#16](https://github.com/lpm-dev/lpm-vault/pull/16) |
| PERF-LOCK-001 | performance_audit | Performance and security | `VaultStore.lock` plaintext clearing | Per-element clearing repeatedly rebuilt every snapshot and approached quadratic work. | The 100-project fixture dropped from 5,907.448–5,962.967 ms to 0.463–0.546 ms. Lock now replaces the project array once. | Verified | Lock behavior tests and A/B benchmark | `76110e8` | Included in [#16](https://github.com/lpm-dev/lpm-vault/pull/16) |
| PERF-UI-003 | performance_audit | Performance | `VaultContentDerivation` | Each render derived both presentation modes and allocated temporary reveal sets. | The 6,000-key fixture dropped from 13.390–13.939 ms to 0.023 ms. The derivation now computes only the active mode. | Verified | `workspace derivation computes only the active presentation mode` and A/B benchmark | `76110e8` | Included in [#16](https://github.com/lpm-dev/lpm-vault/pull/16) |
| PERF-SEARCH-001 | performance_audit | Performance | Sidebar search | Each query scanned and lowercased all keys on the main actor. | The 100,000-key absent query dropped from 8.721–9.137 ms to 6.659–6.885 ms. Snapshots now store one normalized search index. | Verified | Search behavior tests and A/B benchmark | `76110e8` | Included in [#16](https://github.com/lpm-dev/lpm-vault/pull/16) |
| PERF-IMPORT-002 | performance_audit | Performance and peak RAM | Streaming dotenv reader | A newline-free 16 MiB line retained full `Data` and `String` values. | Median time dropped from 440.1–445.8 ms to 25.7–27.4 ms. Peak RSS dropped from 142,983,168–142,999,552 bytes to 40,681,472–40,747,008 bytes. | Verified | Streaming UTF-8, size-limit, parser, and alternating A/B RSS coverage | `76110e8` | Included in [#16](https://github.com/lpm-dev/lpm-vault/pull/16) |
| PERF-KEYCHAIN-002 | performance_audit | Performance | Content-only mutation persistence | One secret edit rewrote an unchanged project index and metadata. | The optimized mutation writes only the project payload when the indexed name and path match. The call-count regression records one payload update. | Verified | `content-only mutation does not rewrite project index metadata` | `76110e8` | Included in [#16](https://github.com/lpm-dev/lpm-vault/pull/16) |
| PERF-LOAD-RAM-001 | performance_audit | Performance and peak RAM | Batched project decode | Raw JSON for all projects stayed live while the service built all decoded projects. | The 500-project fixture dropped from 106.7–107.4 MB RSS to 65.4–66.4 MB RSS. Item-by-item transformation removes the raw-data batch. | Verified | Generated-backend alternating A/B RSS benchmark and real-Keychain project listing benchmark | `76110e8` | Included in [#16](https://github.com/lpm-dev/lpm-vault/pull/16) |
| PERF-ORG-MAIN-001 | performance_audit | Performance | Organization sharing Keychain and keypair paths | Synchronous protected-store and keypair work ran on `MainActor`. | Thread recording reproduced main-thread work. The persistence actor now handles trust data, and detached tasks handle keypair access. | Verified | `organization sharing key lookup executes away from the main thread` and trust failure tests | `76110e8` | Included in [#16](https://github.com/lpm-dev/lpm-vault/pull/16) |
| PERF-CLOUD-UI-001 | performance_audit | Performance | Cloud and organization relative-time rows | Each row created two ISO-8601 formatter objects. | A shared RFC 3339 parser now handles all rows. The reported 10,000-parse analysis was 1.311 seconds with new formatters and 0.250 seconds with reuse. | Verified | Shared fractional, whole-second, invalid, and future timestamp tests | `76110e8` | Included in [#16](https://github.com/lpm-dev/lpm-vault/pull/16) |
| PERF-AVATAR-001 | performance_audit | Performance and peak RAM | `SecureAvatarLoader` image consumers | Repeated avatar loads downloaded and decoded the same image without a memory bound. | Concurrent loads now share one task. A cost-limited and count-limited `NSCache` stores decoded images. | Verified | `avatar image cache coalesces concurrent loads and reuses the decoded image` and avatar policy tests | `76110e8` | Included in [#16](https://github.com/lpm-dev/lpm-vault/pull/16) |
| PERF-AUTH-001B | performance_audit and security_audit | Performance | Cross-process sync authority checks | Each suspension boundary reacquires locks and rereads shared auth state. | Local removal weakens rotation and revocation detection. A cheaper solution requires a shared authenticated generation contract. | Blocked | Contract and race coverage in [issue #14](https://github.com/lpm-dev/lpm-vault/issues/14) | Shared CLI and app auth contract | Tracked by [#14](https://github.com/lpm-dev/lpm-vault/issues/14) |
| PRI-SEARCH-001 | primary_agent | Correctness | Store and sidebar key search | Search examined only the backward-compatible `default` environment. | The base regression missed a key that existed only in another environment. The snapshot index now includes every environment. | Verified | `search includes keys from every environment` | `76110e8` | Included in [#16](https://github.com/lpm-dev/lpm-vault/pull/16) |
| PRI-IMPORT-002 | primary_agent | Correctness | Remote import cancellation after commit | Cancellation after a durable import skipped UI publication and left an invisible duplicate. | A gated base regression returned cancellation after the commit. The result now publishes when the captured session authority is still current. | Verified | `cancellation after a durable cloud import still publishes the committed snapshot` | `76110e8` | Included in [#16](https://github.com/lpm-dev/lpm-vault/pull/16) |

## Benchmark evidence

The A/B tests used the same benchmark source, fixtures, warm gates, and sample counts. The run order alternated between the base and optimized bundles.

| Benchmark | Base median range | Optimized median range | Base peak RSS | Optimized peak RSS |
| --- | ---: | ---: | ---: | ---: |
| Workspace invalidation, 100 projects and 500 keys each | 121.457–127.607 ms | 2.374–2.594 ms | Not a RAM finding | Not a RAM finding |
| Lock clearing, 100 projects and 500 keys each | 5,907.448–5,962.967 ms | 0.463–0.546 ms | Not a RAM finding | Not a RAM finding |
| Sidebar search, 100,000 keys | 8.721–9.137 ms | 6.659–6.885 ms | Not a RAM finding | Not a RAM finding |
| Matrix derivation, 6,000 keys | 13.390–13.939 ms | 0.023 ms | Not a RAM finding | Not a RAM finding |
| Newline-free 16 MiB dotenv import | 440.092–445.766 ms | 25.735–27.373 ms | 142,983,168–142,999,552 bytes | 40,681,472–40,747,008 bytes |
| Generated decode, 500 projects and 75 KB each | 20.748–21.591 ms | 19.846–20.529 ms | 106,725,376–107,380,736 bytes | 65,437,696–66,420,736 bytes |

Preserved release bundles:

- Base: `/tmp/lpm-vault-round2-bench.P61wzU/artifacts/before/LPMVaultPackageTests.xctest`
- Optimized: `/tmp/lpm-vault-round2-bench.P61wzU/artifacts/after/LPMVaultPackageTests.xctest`
- Base SHA-256: `4d8b599baa17a77ef283a97a2bcd960c6e9e90e137eb128e19bc583708e25b88`
- Optimized SHA-256: `328c350e769c54f4abd441dcf8330a6c3e892c190b07218a89530966e4e1eb33`

The base tree contains only the shared benchmark source and a DEBUG-only backend-injection initializer. Production base behavior stays at commit `6751d69`.

## Final verification

- `swift test`: 338 tests passed in 19 suites.
- `swift test --sanitize=thread`: 338 tests passed in 19 suites. Thread Sanitizer reported no race.
- `swift build -c release`: passed.
- `git diff --check`: passed.
- `bench/scripts/run-install-readiness`: unavailable. This repository and the local `lpm-dev` project tree do not contain this runner.
- The primary-agent re-audit found no additional production finding.

## Final totals

- Reports received: 34.
- Canonical ledger items: 32.
- Verified and fixed: 24.
- Rejected with evidence: 4.
- Externally blocked: 4.
- Pending: 0.

The biometric and auth-performance duplicates map to their canonical rows. No report remains unaccounted for.
