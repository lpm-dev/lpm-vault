# End-to-end audit ledger

Concept branch: `codex/end-to-end-audit`  
Base commit: `52ff1cc4589605cf3775359a3d488bc9a112463c`  
Implementation commit: `33a7dd0`  
Concept pull request: [#15](https://github.com/lpm-dev/lpm-vault/pull/15)

This ledger reconciles every lead from the three read-only subagent audits and the primary-agent review. Compound reports are split where one part is locally resolved and another part has an external dependency. Related or duplicate reports remain named in the canonical row.

| ID | Source | Category | Location | Claim | Evidence | Disposition | Coverage | Commit | PR status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| COR-001 | correctness_audit | Correctness | `KeychainService` strict index/project reads; `VaultPersistenceCoordinator.loadSnapshot`; `VaultStore.loadProjects` | Read or decode failures fabricated empty projects and could replace the last coherent UI state. | The old non-throwing path collapsed protected-store and decode errors to `nil`, `[]`, or a synthetic default environment. The strict result path now retains the previous state and reports the error. | Verified | `projectReadFailureFailsClosed`; corrupt duplicate-index integration test; strict Keychain read tests. | `33a7dd0` | Included in [#15](https://github.com/lpm-dev/lpm-vault/pull/15) |
| COR-002 / SEC-META-001 | correctness_audit; security_audit | Correctness; security | Sync metadata and organization-association snapshot, mutation, pull, import, and deletion transactions | Corrupt metadata failed open to empty maps and could erase replay state or route an organization vault as personal. | Malformed durable bytes reproduced the empty-map path. All reads and decodes now fail the complete transaction and preserve the exact prior bytes/state. | Verified | `corruptMetadataFailsClosed`; `corruptOrganizationAssociationFailsClosed`; existing metadata/association rollback and one-shot read-failure tests. | `33a7dd0` | Included in [#15](https://github.com/lpm-dev/lpm-vault/pull/15) |
| COR-003 | correctness_audit | Correctness | `LPMToken`; `AuthSessionTimestamp` | Fractional RFC 3339 timestamps failed to parse and an elapsed token could appear critical rather than expired. | The former formatter rejected fractional seconds and day truncation returned zero after expiry. Static fractional/whole-second strategies plus exact-instant classification now handle both. | Verified | `tokenExpiryParsingAndClassification`; `server RFC 3339 expiry with milliseconds is accepted`; existing expiry filtering test. | `33a7dd0` | Included in [#15](https://github.com/lpm-dev/lpm-vault/pull/15) |
| COR-004 | correctness_audit | Correctness | `UpdateChecker` | Release versions were compared lexicographically. | Numeric assertions reproduce `1.10.0 > 1.9.0`, reject the reverse, and normalize two/three-component equality. | Verified | `numericReleaseComparison`, including the reported `1.9.0`/`1.10.0` boundary. | `33a7dd0` | Included in [#15](https://github.com/lpm-dev/lpm-vault/pull/15) |
| COR-005 / SEC-ENV-001 | correctness_audit; security_audit | Correctness; security | `EnvFileCodec.format` and export callers | Export lost edge tabs and allowed shell expansion when the file was sourced. | `$()`, backticks, and `$HOME` executed or expanded in the former output; tabs were trimmed on re-import. Every value is now escaped and quoted. | Verified | `shellSafeLosslessDotenvExport` executes the export with `/bin/sh`, verifies no marker creation, and verifies lossless codec round-trip. | `33a7dd0` | Included in [#15](https://github.com/lpm-dev/lpm-vault/pull/15) |
| COR-006 | correctness_audit | Correctness | `EnvValidation.decodeRemoteEnvironments`; personal/org import paths | A wholly empty cloud payload persisted no real environment and could not accept a new secret. | The UI synthesized `default` only for display while mutations required it in durable state. Decode now normalizes an empty environment map to `default`. | Verified | `emptyCloudPayloadNormalization`; existing personal/org import and add-secret persistence coverage. | `33a7dd0` | Included in [#15](https://github.com/lpm-dev/lpm-vault/pull/15) |
| COR-007A / SEC-SYNC-001A | correctness_audit; security_audit | Correctness; security | Personal/org list, import, pull, push, and persistence completion | Sync accepted missing/non-positive/non-monotonic versions and mismatched response vault IDs. | Nil versions previously became zero or success and response identity was ignored. All successful responses now require the requested vault ID, a positive version, no downgrade, and push advancement beyond the expected version. | Verified | Personal/org import/pull/push binding tests; `sharedPushResponseValidation`; CLI-mutation-during-push coverage. | `33a7dd0` | Included in [#15](https://github.com/lpm-dev/lpm-vault/pull/15) |
| COR-007B / SEC-SYNC-001B | correctness_audit; security_audit | Security | Sync protocol v1 envelope | Legacy v1 ciphertext does not authenticate the vault ID in AEAD associated data. | Protocol v2 binds the vault ID, and the app now validates response identity, but retiring v1 requires coordinated server and CLI migration. | Blocked | Required substitution/replay/migration vectors are tracked in [issue #13](https://github.com/lpm-dev/lpm-vault/issues/13). | Blocked by server/CLI protocol migration | Tracked by [#13](https://github.com/lpm-dev/lpm-vault/issues/13); local guards included in [#15](https://github.com/lpm-dev/lpm-vault/pull/15) |
| COR-008 | correctness_audit | Correctness | `VaultSecretEditor`; `VaultSecretEditDraft` | A stale editor draft could overwrite a same-key external update. | Same-key value changes did not recreate SwiftUI state. Clean drafts now refresh, dirty drafts enter conflict, and saves stay disabled until resolution. | Verified | `secretDraftConflictHandling`; external update and persistence-failure mutation tests. | `33a7dd0` | Included in [#15](https://github.com/lpm-dev/lpm-vault/pull/15) |
| SEC-KEY-001 | security_audit | Security | `VaultCrypto.stableWrappingKeyUnlocked`; legacy wrapping-key inspection | An unsafe existing wrapping-key file was treated as absent and triggered generation of a replacement key. | Absence and unsafe/malformed/symlink/exposed states formerly shared `nil`. The tri-state inspector now blocks unsafe existence and preserves it for recovery. | Verified | `wrappingKeyFileStates`; legacy key fallback permissions/symlink tests; protected-read failure coverage. | `33a7dd0` | Included in [#15](https://github.com/lpm-dev/lpm-vault/pull/15) |
| SEC-KEY-002 | security_audit | Security | Legacy wrapping-key migration | Successful Keychain migration leaves the filesystem key copy. | Released CLI versions still require the file. App-only deletion would break compatibility and risk data loss. | Blocked | Coordinated verified deletion, conflict, rollback, and interruption coverage is specified in [issue #12](https://github.com/lpm-dev/lpm-vault/issues/12). | Blocked by compatible CLI release/cutover | Tracked by [#12](https://github.com/lpm-dev/lpm-vault/issues/12) |
| SEC-ORG-001 | security_audit | Security | Pending organization key approval and continuation | Continuation accepted an insufficient approval set and stale membership/key/capability data. | Approval arrays now match exactly; after user delay the app refetches its own registered key, member authorization fields, roles, key versions/fingerprints, and replacement capability before wrapping. | Verified | Exact/partial/forged approval test plus `organization approval refetch rejects every changed authorization binding`. | `33a7dd0` | Included in [#15](https://github.com/lpm-dev/lpm-vault/pull/15) |
| SEC-NET-001 | security_audit | Security | Reachable avatar views; `SecureAvatarLoader` | Server-controlled avatar URLs caused arbitrary, unbounded `AsyncImage` requests outside first-party pinning. | Loading is now restricted to approved HTTPS origins/ports, same-origin redirects, a 512 KiB body cap, one image, and 1,024-pixel dimensions; first-party LPM traffic uses the pinning delegate. | Verified | `avatarPolicyAndDimensions`; redirect/origin code-path analysis; shared bounded-response exact/over-limit tests. | `33a7dd0` | Included in [#15](https://github.com/lpm-dev/lpm-vault/pull/15) |
| SEC-KEY-003 | security_audit | Security | `SharedKeychainStore` compatibility/cutover mode | Legacy Keychain remains authoritative until a protected-only cutover marker exists. | This app cannot safely create the marker while supported CLI versions still depend on legacy storage. Local batching retains reconciliation semantics without weakening compatibility. | Blocked | Cross-version cutover, rollback, divergence, and protected-only tests are specified in [issue #12](https://github.com/lpm-dev/lpm-vault/issues/12). | Blocked by compatible CLI release/cutover | Tracked by [#12](https://github.com/lpm-dev/lpm-vault/issues/12) |
| PERF-STARTUP-001 | performance_audit | Performance | App launch/login; `VaultStore.loadAccount` | Startup fetched unused personal and organization token inventories before publishing the user. | Production launch had no inventory UI consumer. Identity-only startup now performs one user request and retains explicit full inventory loading for token operations. | Verified | `startup identity load performs one user request and no token inventory requests`; full inventory tests remain passing. | `33a7dd0` | Included in [#15](https://github.com/lpm-dev/lpm-vault/pull/15) |
| PERF-UI-001 | performance_audit | Performance | `VaultStore.workspaceSnapshots`; workspace/content/inspector/sidebar | Every body pass rebuilt snapshots for all active projects and duplicated the selected snapshot. | Snapshots are now rebuilt only when `projects` changes and reused by all workspace consumers. A/B repeated derivation median fell from 1,638.506 ms to 0.315 ms. | Verified | Workspace snapshot correctness tests and alternating A/B derivation benchmark. | `33a7dd0` | Included in [#15](https://github.com/lpm-dev/lpm-vault/pull/15) |
| PERF-UI-002 | performance_audit | Performance | `VaultContentDerivation`; `VaultContentView`; `VaultSidebarView` | Search/filter/reveal lists were repeatedly allocated within one render. | Content now derives normalized lists/reveal state once per body; sidebar captures one visible-project derivation; status counts reuse cached summaries. | Verified | `VaultContentDerivation` correctness tests plus repeated workspace A/B benchmark. | `33a7dd0` | Included in [#15](https://github.com/lpm-dev/lpm-vault/pull/15) |
| PERF-PERSIST-001 | performance_audit | Performance | `KeychainService.getProjectResult`; single-project persistence paths | A single-project operation decrypted every indexed project and often reread the target. | Direct index metadata lookup plus one target read replaces `listProjects().first(where:)` throughout mutations/import/sync paths. | Verified | `directProjectPersistenceRead` asserts one target read across 100 projects; strict failure test asserts no save. | `33a7dd0` | Included in [#15](https://github.com/lpm-dev/lpm-vault/pull/15) |
| PERF-KEYCHAIN-001 | performance_audit | Performance | `SharedKeychainStore` batched reconciliation | Compatibility marker and reconciliation reads repeated per index item. | The marker is now evaluated once per outer batched transaction while every legacy/shared reconciliation and verification remains intact. | Verified | Exact active/cutover backend call-count tests, repeated-account duplicate handling, and migration rollback suite. | `33a7dd0` | Included in [#15](https://github.com/lpm-dev/lpm-vault/pull/15) |
| PERF-CONCURRENCY-001 | performance_audit | Performance | Personal/org sync JSON, crypto, validation, and project-config work | Large synchronous work ran on `MainActor`. | Bounded config reads, serialization, crypto, and payload decode/merge now execute in detached work; the main actor publishes only validated results. | Verified | `personalSyncCryptoIsBackground`; background config/crypto tests; full TSAN suite. | `33a7dd0` | Included in [#15](https://github.com/lpm-dev/lpm-vault/pull/15) |
| PERF-RAM-001 | performance_audit | Performance | Sync serialization, crypto, and validation | Sync created redundant whole-payload `Data`/`String` copies and duplicate decoding. | Crypto is now data-native and remote environments are decoded once. The 10 MiB sync median improved from 6.927 ms to 5.621 ms and peak RSS from 88.69 MiB to 68.69 MiB. | Verified | Alternating A/B sync benchmark with identical 10 MiB fixture and preserved binaries; crypto interop suite. | `33a7dd0` | Included in [#15](https://github.com/lpm-dev/lpm-vault/pull/15) |
| PERF-NET-001 | performance_audit | Performance | `SyncService.shared`; all sync/import factories | Each operation created a fresh ephemeral session and connection pool. | A locked service registry now retains exactly one pinned session per exact base URL, and all production factories use it. | Verified | `retainedSyncServiceIdentity`; existing pinned session/redirect tests. | `33a7dd0` | Included in [#15](https://github.com/lpm-dev/lpm-vault/pull/15) |
| PERF-NET-002 | performance_audit | Performance | `BoundedHTTPResponse` | The bounded collector performed one `Data.append` for every byte. | Bytes are now accumulated into 64 KiB chunks while the hard limit is checked before admission. The 10 MiB median improved from 189.011 ms to 20.906 ms with unchanged peak RSS. | Verified | Exact/over-limit/missing-length suite and alternating A/B 10 MiB HTTP benchmark. | `33a7dd0` | Included in [#15](https://github.com/lpm-dev/lpm-vault/pull/15) |
| PERF-IMPORT-001 | performance_audit | Performance | `EnvFileImportWorker`; `BoundedEnvFileReader`; streaming parser | Two imports retained complete 16 MiB `Data` and `String` copies despite a roughly 90 KiB parsed-output cap. | Chunked line streaming, batched ignored-line UTF-8 validation, bounded parsed state, and cancellation remove the whole-file copies. Concurrent 16 MiB peak RSS fell from 119.14 MiB to 22.70 MiB. | Verified | Streaming compatibility/UTF-8/limit/cancellation tests and alternating two-import A/B RSS benchmark. | `33a7dd0` | Included in [#15](https://github.com/lpm-dev/lpm-vault/pull/15) |
| PERF-AUTH-001A | performance_audit | Performance | `AuthSessionTimestamp`; `LPMToken` | Expiry parsing allocated one or two ISO-8601 formatter objects per call. | Immutable ISO-8601 format strategies are now reused for fractional and whole-second parsing. | Verified | Fractional/whole timestamp tests; full auth-session transaction suite. | `33a7dd0` | Included in [#15](https://github.com/lpm-dev/lpm-vault/pull/15) |
| PERF-AUTH-001B | performance_audit | Performance | Cross-process auth verification after sync suspension points | One sync repeatedly reacquires locks and reads shared auth state. | Removing those checks locally would allow concurrent CLI rotation/revocation to escape detection. A cheap authenticated generation snapshot needs a shared CLI/app contract. | Blocked | Cross-process generation, rotation/revocation races, and call-count benchmark are specified in [issue #14](https://github.com/lpm-dev/lpm-vault/issues/14). | Blocked by shared CLI/app auth contract | Tracked by [#14](https://github.com/lpm-dev/lpm-vault/issues/14) |
| PRI-CLIP-001 | primary_agent | Correctness; security | `ClipboardManager` delayed clear | A timeout could delete another process's newer clipboard contents. | The former clear was unconditional. The manager now records the successful write's pasteboard change count and clears only while it still owns that generation. | Verified | `clipboardOwnership`; immediate ownership test. | `33a7dd0` | Included in [#15](https://github.com/lpm-dev/lpm-vault/pull/15) |
| PRI-CLIP-003 | primary_agent | Correctness; security | `ClipboardManager` timeout followed by lock/manual clear | After a timeout observed another owner, a later lock could still clear that unrelated value if stale ownership was retained. | Re-review exercised the timeout-then-lock sequence. Ownership is now forgotten on mismatch, and all immediate clears are conditional. Related addendum to `PRI-CLIP-001`. | Verified | `clipboardOwnership` waits for the timeout, then calls immediate clear and verifies the newer value remains. | `33a7dd0` | Included in [#15](https://github.com/lpm-dev/lpm-vault/pull/15) |
| PRI-CLIP-002 | primary_agent | Security | `AppDelegate.applicationWillTerminate` | Quitting could leave a copied secret because the delayed task dies with the process. | Termination did not invoke the synchronous security-state path. It now locks, clears in-memory plaintext, and conditionally clears the owned clipboard before observer teardown. | Verified | `terminationLocksVault`; app lifecycle synchronous-lock tests. | `33a7dd0` | Included in [#15](https://github.com/lpm-dev/lpm-vault/pull/15) |
| PRI-BUILD-001 | primary_agent | Correctness | `build-app.sh` mode parsing | An undeclared mode silently selected Debug. | Any argument other than `release` previously followed the Debug branch despite the declared `release|debug` contract. | Verified | `buildScriptRejectsUnknownMode`; release-tooling suite and ShellCheck. | `33a7dd0` | Included in [#15](https://github.com/lpm-dev/lpm-vault/pull/15) |
| PRI-KEYCHAIN-001 | primary_agent | Correctness; security | Keychain project-index decode and batched reads | Duplicate project IDs could trap `Dictionary(uniqueKeysWithValues:)` or produce ambiguous durable authority. | Re-review injected duplicate identifiers. Index decode now rejects duplicates and unsafe IDs before any dictionary construction or project publication. | Verified | Real Keychain test `a corrupt duplicate project index fails instead of trapping or fabricating projects`; repeated-account batch test. | `33a7dd0` | Included in [#15](https://github.com/lpm-dev/lpm-vault/pull/15) |
| PRI-EDITOR-001 | primary_agent | Correctness | `VaultSecretEditor` durable acknowledgement | An early implementation could mark a save successful before Keychain persistence acknowledged it. | Re-review forced persistence failure. The editor now updates its baseline only when the authoritative project value changes after durable success; failures remain dirty/retryable. | Verified | Pending-save branch in `secretDraftConflictHandling`; existing failed-update persistence tests. | `33a7dd0` | Included in [#15](https://github.com/lpm-dev/lpm-vault/pull/15) |
| PRI-IMPORT-001 | primary_agent | Performance | Streaming dotenv ignored-line validation | The initial streaming implementation validated each tiny comment separately and regressed comment-heavy throughput. | Re-review benchmarked the fixture; ignored ASCII lines are now batched to 64 KiB while invalid UTF-8 remains rejected. Final two-import median is 775.729 ms versus 868.968 ms before. | Verified | Invalid UTF-8 comment test, streaming parser suite, and alternating concurrent-import A/B benchmark. | `33a7dd0` | Included in [#15](https://github.com/lpm-dev/lpm-vault/pull/15) |
| PRI-SYNC-001 | primary_agent | Correctness; security | Personal/org push response validation | A push response equal to the local expected version could be accepted even though the write did not advance concurrency state. | Re-review injected equal/lower versions. Shared validation now requires a positive response version strictly greater than the expected version. | Verified | `sharedPushResponseValidation` and personal/org push response binding tests. | `33a7dd0` | Included in [#15](https://github.com/lpm-dev/lpm-vault/pull/15) |

## Benchmark evidence

The before and after test bundles were built from the same base and optimized source, then run with identical fixtures, deterministic warm gates, five samples, and alternating execution order.

| Benchmark | Before median | After median | Peak RSS before | Peak RSS after |
| --- | ---: | ---: | ---: | ---: |
| 10 MiB sync serialization/crypto | 6.927 ms | 5.621 ms | 88.69 MiB | 68.69 MiB |
| Two concurrent 16 MiB dotenv imports | 868.968 ms | 775.729 ms | 119.14 MiB | 22.70 MiB |
| 10 MiB bounded HTTP body | 189.011 ms | 20.906 ms | 66.88 MiB | 66.89 MiB |
| Repeated workspace derivation | 1,638.506 ms | 0.315 ms | 33.16 MiB | 33.19 MiB |

Preserved bundles:

- Before: `/tmp/lpm-vault-audit-bench.g5kE8t/before-build/arm64-apple-macosx/debug/LPMVaultPackageTests.xctest`
- After: `/tmp/lpm-vault-audit-bench.g5kE8t/after-build/arm64-apple-macosx/debug/LPMVaultPackageTests.xctest`

## Final verification

- `swift test`: 308 tests passed in 18 suites.
- `swift test --sanitize=thread`: 308 tests passed in 18 suites with no reported race.
- `swift build -c release`: passed.
- Release-tooling tests: 40 passed.
- TLS pin audit: passed against the live `lpm.dev` chain.
- Bash syntax and ShellCheck: passed.
- `bench/scripts/run-install-readiness`: not run because shared install/runtime paths did not change.

## Final totals

- Reports received: 35 (27 subagent reports and 8 primary-agent reports).
- Disposition ledger items: 34 (compound sync/auth reports were split; related source duplicates are retained in canonical rows).
- Verified and fixed: 30.
- Rejected with evidence: 0.
- Externally blocked: 4.
- Pending: 0.
